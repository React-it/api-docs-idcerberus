---
title: Índice de services
description: Lista operacional dos services já documentados no API Reference
---

# Índice de services

Use este índice quando já souber qual produto precisa executar e quiser confirmar o nome exato do `service` antes de montar a chamada.

<Info>
Todas as consultas abaixo usam `POST /api/service-api`. O produto executado é definido pelo campo `service` no body.
</Info>

## Pessoa Física

| Nome | Service | Campos principais | Retorno principal |
| --- | --- | --- | --- |
| [Antecedentes criminais civis](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP` | `cpf`, `rg`, `uf` | Retorna resultado de antecedentes criminais civis, com status da certidão, ocorrências encontradas, UF, RG e mensagens da consulta. |
| [Antecedentes criminais federais](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP` | `cpf` | Retorna resultado de antecedentes criminais federais, com status da certidão, ocorrências encontradas e mensagens da consulta. |
| [Certidão de Nada Consta](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP` | `cpf`, `court`, `uf`, `sphere` | Retorna certidão de nada consta para a esfera/tribunal informado, com status, mensagem, ocorrências e dados usados na consulta. |
| [Certidão negativa de protesto](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP` | `cpf` | Retorna certidão/consulta de protestos para CPF, com status de nada consta ou lista de protestos, cartorio, valor e datas. |
| [Certidão negativa de protesto PF (InfoSimples)](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PROTEST_PF_INFOSIMPLES` | `cpf` | Retorna certidão/consulta de protestos para CPF via InfoSimples, com status, cartorios consultados, protestos e mensagens. |
| [Certidão negativa de protesto PF (Netrin)](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PROTEST_PF_NETRIN` | `cpf` | Retorna certidão/consulta de protestos para CPF via Netrin, com status, cartorios consultados, protestos e mensagens. |
| [Consulta de MEI](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_MEI_BIGDATACORP` | `cpf` | Retorna empresas MEI associadas ao CPF, incluindo CNPJ, razão social, situação, atividades, endereço e datas cadastrais quando disponíveis. |
| [CPF na Receita Federal on-demand](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP` | `cpf` | Retorna situação atualizada do CPF consultada sob demanda na Receita Federal, com nome, nascimento, status cadastral e protocolo. |
| [Dados eleitorais de candidato](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP` | `cpf` | Retorna histórico de candidaturas eleitorais do CPF, incluindo cargo, partido, ano, unidade eleitoral, bens declarados e situação quando disponível. |
| [Dados financeiros e endereços](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP` | `cpf`, `birthDate` | Retorna dados financeiros e endereços do CPF em uma consulta combinada, incluindo renda estimada, indicadores financeiros e endereços encontrados. |
| [Dados pelo telefone](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_CONFIRM_PHONE_FACETEC` | `phone` | Retorna dados associados ao telefone informado, como possível titular, documento relacionado, status de confirmacao e atributos disponíveis. |
| [Dados PIS](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PIS_CONSULTATION_BIGDATACORP` | `cpf` | Retorna dados de PIS/NIS associados ao CPF, incluindo número encontrado, status, dados cadastrais relacionados e mensagens da consulta. |
| [Dívida ativa](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_ACTIVE_DEBT_PF_BIGDATACORP` | `cpf` | Retorna dívidas ativas vinculadas ao CPF, com origem do débito, valores, situação, órgão credor e status da consulta. |
| [Doações eleitorais](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP` | `cpf` | Retorna doações eleitorais realizadas pelo CPF, com ano, candidato/partido, valor, cargo, UF e detalhes da prestacao de contas. |
| [Documentoscopia digital](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX` | `key`, `image1`, `image2`, `selfie1` | Retorna status da documentoscopia, chave da consulta, dados extraídos do documento, validações de documento/selfie e resultado de aprovacao. |
| [Endereços](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_ADDRESS_BIGDATACORP` | `cpf` | Retorna endereços associados ao CPF, incluindo logradouro, número, bairro, cidade, UF, CEP, pais, tipo e indicadores de atualidade quando disponíveis. |
| [Enriquecimento de dados](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP` | `cpf` | Retorna dados cadastrais do CPF, incluindo nome, nascimento, situação cadastral, filiação, óbito, idade, gênero e atributos disponíveis. |
| [Envolvimento político](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP` | `cpf` | Retorna envolvimento político do CPF, incluindo candidaturas, cargos, doações, prestações de serviço, partidos e vínculos políticos. |
| [Envolvimento político PF (BigDataCorp)](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP` | `cpf` | Retorna envolvimento político do CPF, incluindo candidaturas, cargos, doações, prestações de serviço, partidos e vínculos políticos. |
| [Exposição e perfil na mídia](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP` | `cpf` | Retorna exposicao e perfil de mídia da pessoa, com noticias, fontes, categorias, sentimento, relevancia e alertas encontrados. |
| [FaceMatch](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_FACE_MATCH_BIGDATACORP` | `image1`, `image2` | Retorna comparacao facial entre duas imagens, com score de similaridade, status do match e mensagem de aprovacao ou reprovacao. |
| [FaceMatch (AWS)](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_FACE_MATCH_AWS` | `image1`, `image2` | Retorna comparacao facial entre duas imagens, com score de similaridade, status do match e mensagem de aprovacao ou reprovacao. |
| [Histórico de e-mails](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_EMAILS_EXTENDED_BIGDATACORP` | `cpf`, `limit` | Retorna e-mails associados ao CPF, incluindo prioridade, status de validação, origem, data de atualização e sinais de uso quando disponíveis. |
| [Histórico de telefones](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PHONE_HISTORY_BIGDATACORP` | `cpf`, `birthDate`, `limit` | Retorna histórico de telefones associados ao CPF, incluindo número, tipo de linha, operadora, prioridade, status e recencia quando disponíveis. |
| [Histórico familiar político](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP` | `cpf` | Retorna histórico político familiar do CPF, incluindo familiares com candidaturas, doações, cargos, partidos e vínculos eleitorais quando encontrados. |
| [Histórico profissional](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP` | `cpf` | Retorna histórico profissional do CPF, incluindo empresas, cargos, datas, vínculos empregaticios ou societários e indicadores profissionais. |
| [Histórico profissional do titular](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP` | `cpf`, `birthDate` | Retorna histórico profissional em que a pessoa aparece como titular, sócio ou proprietario, com empresas, cargos e datas de vinculo. |
| [Informações financeiras](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_FINANCIAL_INFORMATION_BIGDATACORP` | `cpf` | Retorna informações financeiras estimadas do CPF, como renda presumida, poder aquisitivo, classe econômica e indicadores financeiros disponíveis. |
| [KYC e compliance](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PERSON_KYC_BIGDATACORP` | `cpf`, `birthDate` | Retorna checagem de KYC da pessoa, incluindo PEP, sancoes, mídia, processos, alertas de compliance e sinais de risco. |
| [Liveness 2D](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_LIVENESS_2D_FACETEC` | `image1` | Retorna resultado da prova de vida 2D, com status, score ou confiança da selfie e sinais de validação contra fraude simples. |
| [Mandado de prisão](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_ARREST_WARRANT` | `nome`, `motherName`, `fatherName`, `birthDate`, `cpf` | Retorna indicativos de mandado de prisão para os dados informados, com situação, órgão, processo e detalhes encontrados quando houver ocorrência. |
| [Modelagem de dados](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PERSON_DATA_MODELING_BIGDATACORP` | `cpf` | Retorna modelagem consolidada da pessoa, reunindo dados cadastrais, contatos, endereços, vínculos, indicadores e resumos derivados. |
| [OCR de documentos](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_OCR_BIGDATACORP` | `image1`, `image2`, `image1Url`, `image2Url` | Retorna dados extraídos das imagens do documento, como tipo documental, nome, CPF, nascimento, filiação, número do documento e campos específicos. |
| [Pessoa politicamente exposta](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PEP` | `cpf` | Retorna se o CPF e PEP ou relacionado a PEP, com cargo, órgão, nivel de exposicao, periodo e vínculos encontrados quando disponíveis. |
| [Pessoas relacionadas](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_RELATED_PEOPLE_BIGDATACORP` | `cpf`, `birthDate` | Retorna pessoas relacionadas ao CPF, com nome, documento mascarado, tipo de relacao, nivel de proximidade e origem do vinculo. |
| [Prestadores de serviço eleitorais](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP` | `cpf` | Retorna prestações de serviço eleitorais vinculadas ao CPF, com campanha, candidato/partido, valor, ano e natureza do serviço. |
| [Processos jurídicos e administrativos](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_JURIDICAL_PROCESSES_BIGDATACORP` | `cpf` | Retorna processos jurídicos e administrativos vinculados ao CPF, com tribunal, classe, assunto, partes, status e datas quando disponíveis. |
| [Prompt de IA para pessoa](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PERSON_AI_PROMPT_OPENAI` | `cpf` | Retorna uma resposta textual consolidada por IA a partir dos dados da pessoa, com resumo, pontos de atenção e leitura operacional. |
| [Propensão a apostas online](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP` | `cpf` | Retorna propensão do CPF a apostas online, com score, faixa de propensão, indicadores comportamentais e sinais associados quando disponíveis. |
| [Relacionamentos econômicos](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP` | `cpf` | Retorna vínculos econômicos associados ao CPF, como empresas relacionadas, participacoes, relacoes profissionais e indicadores de relacionamento. |
| [Resultado da documentoscopia digital](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX` | `key` | Retorna o resultado ja processado da documentoscopia pela chave informada, com status, campos extraídos, regras avaliadas e evidencias. |
| [Risco financeiro](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP` | `cpf`, `birthDate` | Retorna score de risco financeiro do CPF, faixa de risco, recomendação resumida e fatores que influenciam a avaliacao. |
| [Score biométrico](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_DATAVALID_CNH_SERPRO` | `cpf`, `image1` | Retorna validação DataValid/Serpro da CNH, incluindo score biométrico, similaridade facial, status de validação e campos conferidos. |
| [Score de inadimplência](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP` | `cpf` | Retorna score de inadimplencia do CPF, faixa de risco, probabilidade estimada e indicadores usados na avaliacao. |
| [Score de risco de fraude](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_FRAUD_RISK_SCORE_BIGDATACORP` | `cpf`, `factor` | Retorna score de risco de fraude do CPF, fator analisado, nivel de risco, score numerico e sinais que suportam a decisão. |
| [Servidores públicos](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PUBLIC_SERVANTS_BIGDATACORP` | `cpf` | Retorna registros de servidor publico associados ao CPF, incluindo órgão, cargo, vinculo, remuneracao/faixa e periodo quando disponíveis. |
| [Status do CPF na Receita Federal](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_RFB_PF_BIGDATACORP` | `cpf`, `dataDeNascimento` | Retorna situação do CPF na Receita Federal, incluindo nome, nascimento, status cadastral, comprovante/protocolo e dados fiscais disponíveis. |
| [Validação de CPF com endereço](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP` | `cpf`, `zipcode`, `numberAddress` | Retorna se o endereço informado tem associação com o CPF, incluindo nivel de match, endereço normalizado e sinais usados na validação. |
| [Validação de CPF com telefone](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP` | `cpf`, `phone` | Retorna se o telefone informado tem associação com o CPF, incluindo nivel de match, tipo de linha, status e sinais de validação. |
| [Validação de CPF com telefone (Facetec)](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_CPF_PHONE_VALIDATION_FACETEC` | `cpf`, `phone` | Retorna validação da associação entre CPF e telefone, com status de match, mensagem da consulta e dados retornados pelo parceiro. |
| [Validação de e-mail](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_EMAIL_VALIDATION_BIGDATACORP` | `email` | Retorna validação do e-mail informado, incluindo formato, existencia provável, dominio, entregabilidade e indicadores de risco. |
| [Validação do E-Social](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP` | `cpf`, `nit` | Retorna qualificação cadastral no eSocial, com status de consistencia entre CPF, NIT/PIS e dados cadastrais informados. |
## Pessoa Jurídica

| Nome | Service | Campos principais | Retorno principal |
| --- | --- | --- | --- |
| [Certidão negativa de protesto](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PROTEST_PJ_INFOSIMPLES` | `cnpj` | Retorna certidão/consulta de protestos para CNPJ via InfoSimples, com status, cartorios consultados, protestos, valores e datas. |
| [Certidão negativa de protesto PJ (Netrin)](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_PROTEST_PJ_NETRIN` | `cnpj` | Retorna certidão/consulta de protestos para CNPJ via Netrin, com status, cartorios consultados, protestos, valores e datas. |
| [CNPJ na Receita Federal on-demand](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP` | `cnpj` | Retorna situação atualizada do CNPJ consultada sob demanda na Receita Federal, com razão social, status cadastral, CNAEs e endereço. |
| [Compliance de casas de apostas](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP` | `cnpj` | Retorna indicadores de exposicao da empresa a apostas, bets e compliance regulatório, incluindo sinais de operação, dominio, atividade e alertas. |
| [DAS MEI na Receita](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_DAS_MEI_INFOSIMPLES` | `cnpj` | Retorna informações de DAS MEI e situação fiscal relacionada ao CNPJ, incluindo periodos, pagamentos, pendencias e status quando disponíveis. |
| [Débitos ativos](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP` | `cnpj` | Retorna dívidas ativas vinculadas ao CNPJ, com origem do débito, valores, situação, órgão credor e status da consulta. |
| [Doações eleitorais](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP` | `cnpj` | Retorna doações eleitorais realizadas pela empresa, com ano, candidato/partido, valor, cargo, UF e detalhes da prestacao de contas. |
| [Doações eleitorais dos sócios](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP` | `cnpj` | Retorna doações eleitorais feitas pelos sócios da empresa, com sócio relacionado, ano, candidato/partido, valor e detalhes eleitorais. |
| [Endereços estendidos](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP` | `cnpj` | Retorna endereços associados ao CNPJ, incluindo logradouro, bairro, cidade, UF, CEP, pais, tipo de endereço e dados complementares quando disponíveis. |
| [Enriquecimento de dados](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP` | `cnpj` | Retorna cadastro completo da empresa, incluindo razão social, nome fantasia, situação cadastral, CNAEs, natureza juridica, porte, capital e endereço. |
| [Exposição e perfil na mídia dos sócios](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP` | `cnpj` | Retorna exposicao e perfil de mídia da empresa e sócios, com noticias, fontes, categorias, sentimento, relevancia e alertas encontrados. |
| [Fornecedores eleitorais](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP` | `cnpj` | Retorna prestações de serviço eleitorais vinculadas ao CNPJ, com campanha, candidato/partido, valor, ano e natureza do serviço. |
| [KYC e compliance dos sócios](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP` | `cnpj` | Retorna checagens de KYC e compliance dos sócios da empresa, incluindo PEP, sancoes, mídia, risco e alertas encontrados por sócio. |
| [Processos jurídicos dos sócios](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP` | `cnpj` | Retorna processos jurídicos associados aos sócios da empresa, com sócio relacionado, tribunal, classe, assunto, status e datas. |
| [Relacionamentos da empresa](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP` | `cnpj` | Retorna relacionamentos da empresa, como sócios, proprietarios, empresas relacionadas, participacoes e vínculos societários identificados. |
| [SINTEGRA](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP` | `cnpj`, `uf` | Retorna dados do SINTEGRA, incluindo inscrição estadual, UF, situação, regime, atividades, endereço e mensagens da consulta. |
| [Sócios de primeiro nível](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP` | `cnpj` | Retorna sócios de primeiro nivel da empresa, com nome, documento, participação, qualificação e vínculos diretos ao CNPJ. |
| [Sócios na Receita Federal](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP` | `cnpj` | Retorna o quadro societario na Receita Federal, com nome dos sócios, documentos mascarados, qualificação, participação e data de entrada quando disponível. |
| [Status do CNPJ na Receita Federal](https://api-docs.idcerberus.com/api-reference/servi%C3%A7os--pessoas/executar-servi%C3%A7o-de-dados-risco-ou-compliance) | `SERVICE_RFB_PJ_BIGDATACORP` | `cnpj` | Retorna situação do CNPJ na Receita Federal, incluindo razão social, nome fantasia, situação cadastral, abertura, CNAEs e endereço. |

## Passo a passo por service

Use esta parte quando precisar explicar para o cliente exatamente o que enviar e o que esperar de volta em cada produto.

### Pessoa Física

<AccordionGroup>
<Accordion title="Antecedentes criminais civis">

**Service:** `SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP`

**Quando usar:** Use para consultar certidões, processos ou informações jurídicas da pessoa.

**O que retorna:** Retorna resultado de antecedentes criminais civis, com status da certidão, ocorrências encontradas, UF, RG e mensagens da consulta.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf`, `rg`, `uf` junto com `service: SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `rg`, `state`, `hasRecords`, `records`.

**Body exemplo:**

```json
{
  "service": "SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP",
  "cpf": "cpf",
  "rg": "rg",
  "uf": "uf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "rg": "rg",
    "state": "SP",
    "hasRecords": false,
    "records": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Antecedentes criminais federais">

**Service:** `SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP`

**Quando usar:** Use para consultar certidões, processos ou informações jurídicas da pessoa.

**O que retorna:** Retorna resultado de antecedentes criminais federais, com status da certidão, ocorrências encontradas e mensagens da consulta.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `hasFederalCriminalRecord`, `records`.

**Body exemplo:**

```json
{
  "service": "SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "hasFederalCriminalRecord": false,
    "records": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Certidão de Nada Consta">

**Service:** `SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP`

**Quando usar:** Use para consultar certidões, processos ou informações jurídicas da pessoa.

**O que retorna:** Retorna certidão de nada consta para a esfera/tribunal informado, com status, mensagem, ocorrências e dados usados na consulta.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf`, `court`, `uf`, `sphere` junto com `service: SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `court`, `sphere`, `nothingFound`, `records`.

**Body exemplo:**

```json
{
  "service": "SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP",
  "cpf": "cpf",
  "court": "TRF1",
  "uf": "uf",
  "sphere": "CIVIL"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "court": "TRF1",
    "sphere": "CIVIL",
    "nothingFound": true,
    "records": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Certidão negativa de protesto">

**Service:** `SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP`

**Quando usar:** Use para consultar protestos associados ao documento da pessoa.

**O que retorna:** Retorna certidão/consulta de protestos para CPF, com status de nada consta ou lista de protestos, cartorio, valor e datas.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `hasProtests`, `protests`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "hasProtests": false,
    "protests": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Certidão negativa de protesto PF (InfoSimples)">

**Service:** `SERVICE_PROTEST_PF_INFOSIMPLES`

**Quando usar:** Use para consultar protestos associados ao documento da pessoa.

**O que retorna:** Retorna certidão/consulta de protestos para CPF via InfoSimples, com status, cartorios consultados, protestos e mensagens.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PROTEST_PF_INFOSIMPLES` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `hasProtests`, `notaryOffices`, `protests`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PROTEST_PF_INFOSIMPLES",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "hasProtests": false,
    "notaryOffices": [],
    "protests": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Certidão negativa de protesto PF (Netrin)">

**Service:** `SERVICE_PROTEST_PF_NETRIN`

**Quando usar:** Use para consultar protestos associados ao documento da pessoa.

**O que retorna:** Retorna certidão/consulta de protestos para CPF via Netrin, com status, cartorios consultados, protestos e mensagens.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PROTEST_PF_NETRIN` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `hasProtests`, `notaryOffices`, `protests`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PROTEST_PF_NETRIN",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "hasProtests": false,
    "notaryOffices": [],
    "protests": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Consulta de MEI">

**Service:** `SERVICE_MEI_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Consulta de MEI" via API.

**O que retorna:** Retorna empresas MEI associadas ao CPF, incluindo CNPJ, razão social, situação, atividades, endereço e datas cadastrais quando disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_MEI_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `meiCompanies`.

**Body exemplo:**

```json
{
  "service": "SERVICE_MEI_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "meiCompanies": [
      {
        "cnpj": "cnpj",
        "officialName": "MEI EXEMPLO",
        "status": "ATIVA"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="CPF na Receita Federal on-demand">

**Service:** `SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP`

**Quando usar:** Use para consultar ou validar dados cadastrais da pessoa em bases da Receita Federal.

**O que retorna:** Retorna situação atualizada do CPF consultada sob demanda na Receita Federal, com nome, nascimento, status cadastral e protocolo.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `name`, `birthDate`, `registrationStatus`, `protocol`.

**Body exemplo:**

```json
{
  "service": "SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "name": "Nome completo",
    "birthDate": "yyyy-MM-dd",
    "registrationStatus": "REGULAR",
    "protocol": "protocolo"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Dados eleitorais de candidato">

**Service:** `SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP`

**Quando usar:** Use para consultar informações eleitorais relacionadas à pessoa.

**O que retorna:** Retorna histórico de candidaturas eleitorais do CPF, incluindo cargo, partido, ano, unidade eleitoral, bens declarados e situação quando disponível.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `candidacies`.

**Body exemplo:**

```json
{
  "service": "SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "candidacies": [
      {
        "year": 2024,
        "role": "VEREADOR",
        "party": "PARTIDO",
        "status": "DEFERIDO"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Dados financeiros e endereços">

**Service:** `SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP`

**Quando usar:** Use para consultar informações financeiras associadas à pessoa.

**O que retorna:** Retorna dados financeiros e endereços do CPF em uma consulta combinada, incluindo renda estimada, indicadores financeiros e endereços encontrados.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `estimatedIncome`, `addresses`, `financialIndicators`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "estimatedIncome": "5000-10000",
    "addresses": [
      {
        "city": "Sao Paulo",
        "state": "SP"
      }
    ],
    "financialIndicators": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Dados pelo telefone">

**Service:** `SERVICE_CONFIRM_PHONE_FACETEC`

**Quando usar:** Use para consultar, validar ou enriquecer dados de telefone.

**O que retorna:** Retorna dados associados ao telefone informado, como possível titular, documento relacionado, status de confirmacao e atributos disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `phone` junto com `service: SERVICE_CONFIRM_PHONE_FACETEC` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `phone`, `matched`, `person`.

**Body exemplo:**

```json
{
  "service": "SERVICE_CONFIRM_PHONE_FACETEC",
  "phone": "+5561123456789"
}
```

**Response resumido:**

```json
{
  "result": {
    "phone": "+5561123456789",
    "matched": true,
    "person": {
      "name": "Nome encontrado",
      "document": "cpf"
    }
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Dados PIS">

**Service:** `SERVICE_PIS_CONSULTATION_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Dados PIS" via API.

**O que retorna:** Retorna dados de PIS/NIS associados ao CPF, incluindo número encontrado, status, dados cadastrais relacionados e mensagens da consulta.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PIS_CONSULTATION_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `pis`, `status`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PIS_CONSULTATION_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "pis": "00000000000",
    "status": "FOUND"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Dívida ativa">

**Service:** `SERVICE_ACTIVE_DEBT_PF_BIGDATACORP`

**Quando usar:** Use para consultar débitos ou dívidas associadas à pessoa.

**O que retorna:** Retorna dívidas ativas vinculadas ao CPF, com origem do débito, valores, situação, órgão credor e status da consulta.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_ACTIVE_DEBT_PF_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `totalDebts`, `totalValue`, `debts`.

**Body exemplo:**

```json
{
  "service": "SERVICE_ACTIVE_DEBT_PF_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "totalDebts": 2,
    "totalValue": "1234.56",
    "debts": [
      {
        "source": "PGFN",
        "value": "1234.56",
        "status": "ACTIVE"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Doações eleitorais">

**Service:** `SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP`

**Quando usar:** Use para consultar informações eleitorais relacionadas à pessoa.

**O que retorna:** Retorna doações eleitorais realizadas pelo CPF, com ano, candidato/partido, valor, cargo, UF e detalhes da prestacao de contas.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `donations`.

**Body exemplo:**

```json
{
  "service": "SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "donations": [
      {
        "year": 2024,
        "recipient": "Candidato",
        "amount": "500.00"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Documentoscopia digital">

**Service:** `SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX`

**Quando usar:** Use para avaliar documento, selfie e biometria dentro do fluxo de documentoscopia.

**O que retorna:** Retorna status da documentoscopia, chave da consulta, dados extraídos do documento, validações de documento/selfie e resultado de aprovacao.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `key`, `image1`, `image2`, `selfie1` junto com `service: SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `key`, `status`, `documentData`, `validations`.

**Body exemplo:**

```json
{
  "service": "SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX",
  "key": "84bfcd2e-2336-4e30-bcab-15348b7890b5",
  "image1": "base64",
  "image2": "base64",
  "selfie1": "base64"
}
```

**Response resumido:**

```json
{
  "result": {
    "key": "{key}",
    "status": "APPROVED",
    "documentData": {
      "name": "Nome extraido",
      "cpf": "cpf"
    },
    "validations": [
      {
        "name": "faceMatch",
        "status": "APPROVED"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Endereços">

**Service:** `SERVICE_ADDRESS_BIGDATACORP`

**Quando usar:** Use para consultar ou validar endereços associados ao documento.

**O que retorna:** Retorna endereços associados ao CPF, incluindo logradouro, número, bairro, cidade, UF, CEP, pais, tipo e indicadores de atualidade quando disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_ADDRESS_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `totalAddresses`, `addresses`.

**Body exemplo:**

```json
{
  "service": "SERVICE_ADDRESS_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "totalAddresses": 2,
    "addresses": [
      {
        "address": "Rua Exemplo",
        "number": "100",
        "neighborhood": "Centro",
        "city": "Sao Paulo",
        "state": "SP",
        "zipcode": "01001000"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Enriquecimento de dados">

**Service:** `SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP`

**Quando usar:** Use para complementar dados cadastrais da pessoa a partir do documento informado.

**O que retorna:** Retorna dados cadastrais do CPF, incluindo nome, nascimento, situação cadastral, filiação, óbito, idade, gênero e atributos disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `name`, `birthDate`, `registrationStatus`, `motherName`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "name": "Nome completo",
    "birthDate": "yyyy-MM-dd",
    "registrationStatus": "REGULAR",
    "motherName": "Nome da mae"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Envolvimento político">

**Service:** `SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Envolvimento político" via API.

**O que retorna:** Retorna envolvimento político do CPF, incluindo candidaturas, cargos, doações, prestações de serviço, partidos e vínculos políticos.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `politicalInvolvement`.

**Body exemplo:**

```json
{
  "service": "SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "politicalInvolvement": [
      {
        "type": "CANDIDACY",
        "year": 2024,
        "details": "Candidatura encontrada"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Envolvimento político PF (BigDataCorp)">

**Service:** `SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Envolvimento político PF (BigDataCorp)" via API.

**O que retorna:** Retorna envolvimento político do CPF, incluindo candidaturas, cargos, doações, prestações de serviço, partidos e vínculos políticos.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `politicalInvolvement`.

**Body exemplo:**

```json
{
  "service": "SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "politicalInvolvement": [
      {
        "type": "DONATION",
        "year": 2024,
        "details": "Doacao encontrada"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Exposição e perfil na mídia">

**Service:** `SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Exposição e perfil na mídia" via API.

**O que retorna:** Retorna exposicao e perfil de mídia da pessoa, com noticias, fontes, categorias, sentimento, relevancia e alertas encontrados.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `mediaMentions`, `exposureLevel`.

**Body exemplo:**

```json
{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "mediaMentions": [
      {
        "title": "Noticia encontrada",
        "source": "Fonte",
        "sentiment": "NEUTRAL"
      }
    ],
    "exposureLevel": "LOW"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="FaceMatch">

**Service:** `SERVICE_FACE_MATCH_BIGDATACORP`

**Quando usar:** Use para comparar duas imagens faciais e retornar a similaridade entre elas.

**O que retorna:** Retorna comparacao facial entre duas imagens, com score de similaridade, status do match e mensagem de aprovacao ou reprovacao.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `image1`, `image2` junto com `service: SERVICE_FACE_MATCH_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `match`, `similarity`, `status`.

**Body exemplo:**

```json
{
  "service": "SERVICE_FACE_MATCH_BIGDATACORP",
  "image1": "base64",
  "image2": "base64"
}
```

**Response resumido:**

```json
{
  "result": {
    "match": true,
    "similarity": 98.2,
    "status": "APPROVED"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="FaceMatch (AWS)">

**Service:** `SERVICE_FACE_MATCH_AWS`

**Quando usar:** Use para comparar duas imagens faciais e retornar a similaridade entre elas.

**O que retorna:** Retorna comparacao facial entre duas imagens, com score de similaridade, status do match e mensagem de aprovacao ou reprovacao.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `image1`, `image2` junto com `service: SERVICE_FACE_MATCH_AWS` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `match`, `similarity`, `status`.

**Body exemplo:**

```json
{
  "service": "SERVICE_FACE_MATCH_AWS",
  "image1": "base64",
  "image2": "base64"
}
```

**Response resumido:**

```json
{
  "result": {
    "match": true,
    "similarity": 98.2,
    "status": "APPROVED"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Histórico de e-mails">

**Service:** `SERVICE_EMAILS_EXTENDED_BIGDATACORP`

**Quando usar:** Use para validar ou consultar histórico de e-mails relacionados ao documento.

**O que retorna:** Retorna e-mails associados ao CPF, incluindo prioridade, status de validação, origem, data de atualização e sinais de uso quando disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf`, `limit` junto com `service: SERVICE_EMAILS_EXTENDED_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `emails`.

**Body exemplo:**

```json
{
  "service": "SERVICE_EMAILS_EXTENDED_BIGDATACORP",
  "cpf": "cpf",
  "limit": "10"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "emails": [
      {
        "email": "email@exemplo.com",
        "priority": 1,
        "isValid": true,
        "lastUpdate": "yyyy-MM-dd"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Histórico de telefones">

**Service:** `SERVICE_PHONE_HISTORY_BIGDATACORP`

**Quando usar:** Use para consultar, validar ou enriquecer dados de telefone.

**O que retorna:** Retorna histórico de telefones associados ao CPF, incluindo número, tipo de linha, operadora, prioridade, status e recencia quando disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf`, `limit` junto com `service: SERVICE_PHONE_HISTORY_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `phones`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PHONE_HISTORY_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)",
  "limit": "10"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "phones": [
      {
        "phone": "11900000000",
        "lineType": "MOBILE",
        "priority": 1,
        "lastUpdate": "yyyy-MM-dd"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Histórico familiar político">

**Service:** `SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Histórico familiar político" via API.

**O que retorna:** Retorna histórico político familiar do CPF, incluindo familiares com candidaturas, doações, cargos, partidos e vínculos eleitorais quando encontrados.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `familyPoliticalHistory`.

**Body exemplo:**

```json
{
  "service": "SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "familyPoliticalHistory": [
      {
        "relativeName": "Nome relacionado",
        "relationship": "PARENTE",
        "role": "Candidato"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Histórico profissional">

**Service:** `SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Histórico profissional" via API.

**O que retorna:** Retorna histórico profissional do CPF, incluindo empresas, cargos, datas, vínculos empregaticios ou societários e indicadores profissionais.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `professionalHistory`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "professionalHistory": [
      {
        "companyName": "Empresa Exemplo",
        "role": "Analista",
        "startDate": "yyyy-MM-dd"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Histórico profissional do titular">

**Service:** `SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Histórico profissional do titular" via API.

**O que retorna:** Retorna histórico profissional em que a pessoa aparece como titular, sócio ou proprietario, com empresas, cargos e datas de vinculo.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `ownerHistory`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "ownerHistory": [
      {
        "companyName": "Empresa Exemplo",
        "cnpj": "cnpj",
        "qualification": "SOCIO"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Informações financeiras">

**Service:** `SERVICE_FINANCIAL_INFORMATION_BIGDATACORP`

**Quando usar:** Use para consultar informações financeiras associadas à pessoa.

**O que retorna:** Retorna informações financeiras estimadas do CPF, como renda presumida, poder aquisitivo, classe econômica e indicadores financeiros disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_FINANCIAL_INFORMATION_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `estimatedIncome`, `purchasingPower`, `financialIndicators`.

**Body exemplo:**

```json
{
  "service": "SERVICE_FINANCIAL_INFORMATION_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "estimatedIncome": "5000-10000",
    "purchasingPower": "MEDIUM",
    "financialIndicators": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="KYC e compliance">

**Service:** `SERVICE_PERSON_KYC_BIGDATACORP`

**Quando usar:** Use para executar checagens de KYC e compliance da pessoa.

**O que retorna:** Retorna checagem de KYC da pessoa, incluindo PEP, sancoes, mídia, processos, alertas de compliance e sinais de risco.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PERSON_KYC_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `isPep`, `sanctions`, `mediaExposure`, `riskAlerts`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PERSON_KYC_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "isPep": false,
    "sanctions": [],
    "mediaExposure": [],
    "riskAlerts": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Liveness 2D">

**Service:** `SERVICE_LIVENESS_2D_FACETEC`

**Quando usar:** Use para validar prova de vida a partir de uma imagem de selfie.

**O que retorna:** Retorna resultado da prova de vida 2D, com status, score ou confiança da selfie e sinais de validação contra fraude simples.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `image1` junto com `service: SERVICE_LIVENESS_2D_FACETEC` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `liveness`, `confidence`, `status`.

**Body exemplo:**

```json
{
  "service": "SERVICE_LIVENESS_2D_FACETEC",
  "image1": "selfie"
}
```

**Response resumido:**

```json
{
  "result": {
    "liveness": true,
    "confidence": 0.97,
    "status": "APPROVED"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Mandado de prisão">

**Service:** `SERVICE_ARREST_WARRANT`

**Quando usar:** Use este service quando precisar executar a consulta "Mandado de prisão" via API.

**O que retorna:** Retorna indicativos de mandado de prisão para os dados informados, com situação, órgão, processo e detalhes encontrados quando houver ocorrência.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `nome`, `motherName`, `fatherName`, `birthDate`, `cpf` junto com `service: SERVICE_ARREST_WARRANT` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `hasArrestWarrant`, `warrants`.

**Body exemplo:**

```json
{
  "service": "SERVICE_ARREST_WARRANT",
  "nome": "nome",
  "motherName": "nome da mãe",
  "fatherName": "nome do pai",
  "birthDate": "dd/MM/yyyy",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "hasArrestWarrant": false,
    "warrants": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Modelagem de dados">

**Service:** `SERVICE_PERSON_DATA_MODELING_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Modelagem de dados" via API.

**O que retorna:** Retorna modelagem consolidada da pessoa, reunindo dados cadastrais, contatos, endereços, vínculos, indicadores e resumos derivados.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PERSON_DATA_MODELING_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `profileSummary`, `contacts`, `addresses`, `relationships`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PERSON_DATA_MODELING_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "profileSummary": "Resumo consolidado",
    "contacts": [],
    "addresses": [],
    "relationships": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="OCR de documentos">

**Service:** `SERVICE_OCR_BIGDATACORP`

**Quando usar:** Use para extrair dados de documentos enviados em base64 ou por URL.

**O que retorna:** Retorna dados extraídos das imagens do documento, como tipo documental, nome, CPF, nascimento, filiação, número do documento e campos específicos.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `image1`, `image2`, `image1Url`, `image2Url` junto com `service: SERVICE_OCR_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `documentType`, `fields`, `extractionStatus`.

**Body exemplo:**

```json
{
  "service": "SERVICE_OCR_BIGDATACORP",
  "image1": "base64",
  "image2": "base64",
  "image1Url": "url_image",
  "image2Url": "urlImageMatch"
}
```

**Response resumido:**

```json
{
  "result": {
    "documentType": "CNH",
    "fields": {
      "name": "Nome extraido",
      "cpf": "cpf",
      "birthDate": "yyyy-MM-dd"
    },
    "extractionStatus": "SUCCESS"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Pessoa politicamente exposta">

**Service:** `SERVICE_PEP`

**Quando usar:** Use para verificar exposição política ou vínculo com Pessoa Politicamente Exposta.

**O que retorna:** Retorna se o CPF e PEP ou relacionado a PEP, com cargo, órgão, nivel de exposicao, periodo e vínculos encontrados quando disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PEP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `isPep`, `pepLevel`, `positions`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PEP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "isPep": false,
    "pepLevel": null,
    "positions": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Pessoas relacionadas">

**Service:** `SERVICE_RELATED_PEOPLE_BIGDATACORP`

**Quando usar:** Use para consultar vínculos, sócios ou relacionamentos associados à pessoa.

**O que retorna:** Retorna pessoas relacionadas ao CPF, com nome, documento mascarado, tipo de relacao, nivel de proximidade e origem do vinculo.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_RELATED_PEOPLE_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `relatedPeople`.

**Body exemplo:**

```json
{
  "service": "SERVICE_RELATED_PEOPLE_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "relatedPeople": [
      {
        "name": "Pessoa relacionada",
        "relationshipType": "FAMILIAR",
        "confidence": "HIGH"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Prestadores de serviço eleitorais">

**Service:** `SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP`

**Quando usar:** Use para consultar informações eleitorais relacionadas à pessoa.

**O que retorna:** Retorna prestações de serviço eleitorais vinculadas ao CPF, com campanha, candidato/partido, valor, ano e natureza do serviço.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `providers`.

**Body exemplo:**

```json
{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "providers": [
      {
        "year": 2024,
        "campaign": "Campanha",
        "amount": "800.00",
        "serviceType": "Servico"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Processos jurídicos e administrativos">

**Service:** `SERVICE_JURIDICAL_PROCESSES_BIGDATACORP`

**Quando usar:** Use para consultar certidões, processos ou informações jurídicas da pessoa.

**O que retorna:** Retorna processos jurídicos e administrativos vinculados ao CPF, com tribunal, classe, assunto, partes, status e datas quando disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_JURIDICAL_PROCESSES_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `totalProcesses`, `processes`.

**Body exemplo:**

```json
{
  "service": "SERVICE_JURIDICAL_PROCESSES_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "totalProcesses": 1,
    "processes": [
      {
        "court": "TJSP",
        "processNumber": "0000000-00.0000.0.00.0000",
        "status": "ACTIVE"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Prompt de IA para pessoa">

**Service:** `SERVICE_PERSON_AI_PROMPT_OPENAI`

**Quando usar:** Use este service quando precisar executar a consulta "Prompt de IA para pessoa" via API.

**O que retorna:** Retorna uma resposta textual consolidada por IA a partir dos dados da pessoa, com resumo, pontos de atenção e leitura operacional.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PERSON_AI_PROMPT_OPENAI` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `answer`, `highlights`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PERSON_AI_PROMPT_OPENAI",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "answer": "Resumo analitico gerado pela IA",
    "highlights": [
      "ponto relevante"
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Propensão a apostas online">

**Service:** `SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Propensão a apostas online" via API.

**O que retorna:** Retorna propensão do CPF a apostas online, com score, faixa de propensão, indicadores comportamentais e sinais associados quando disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `propensityScore`, `propensityLevel`, `indicators`.

**Body exemplo:**

```json
{
  "service": "SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "propensityScore": 78,
    "propensityLevel": "HIGH",
    "indicators": [
      "sinal encontrado"
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Relacionamentos econômicos">

**Service:** `SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP`

**Quando usar:** Use para consultar vínculos, sócios ou relacionamentos associados à pessoa.

**O que retorna:** Retorna vínculos econômicos associados ao CPF, como empresas relacionadas, participacoes, relacoes profissionais e indicadores de relacionamento.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `relationships`.

**Body exemplo:**

```json
{
  "service": "SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "relationships": [
      {
        "type": "OWNER",
        "relatedDocument": "cnpj",
        "relatedName": "Empresa relacionada"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Resultado da documentoscopia digital">

**Service:** `SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX`

**Quando usar:** Use para avaliar documento, selfie e biometria dentro do fluxo de documentoscopia.

**O que retorna:** Retorna o resultado ja processado da documentoscopia pela chave informada, com status, campos extraídos, regras avaliadas e evidencias.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `key` junto com `service: SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `key`, `status`, `fields`, `rules`.

**Body exemplo:**

```json
{
  "service": "SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX",
  "key": "de0cd562-5962-40bd-8f94-5a7184ecde0e"
}
```

**Response resumido:**

```json
{
  "result": {
    "key": "{key}",
    "status": "APPROVED",
    "fields": [
      {
        "name": "cpf",
        "value": "cpf"
      }
    ],
    "rules": [
      {
        "name": "document",
        "status": "APPROVED"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Risco financeiro">

**Service:** `SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP`

**Quando usar:** Use para consultar informações financeiras associadas à pessoa.

**O que retorna:** Retorna score de risco financeiro do CPF, faixa de risco, recomendação resumida e fatores que influenciam a avaliacao.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `score`, `riskLevel`, `recommendation`.

**Body exemplo:**

```json
{
  "service": "SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "score": 681,
    "riskLevel": "MEDIUM",
    "recommendation": "REVIEW"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Score biométrico">

**Service:** `SERVICE_DATAVALID_CNH_SERPRO`

**Quando usar:** Use para comparar a imagem enviada com bases biométricas disponíveis e retornar a similaridade.

**O que retorna:** Retorna validação DataValid/Serpro da CNH, incluindo score biométrico, similaridade facial, status de validação e campos conferidos.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf`, `image1` junto com `service: SERVICE_DATAVALID_CNH_SERPRO` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `biometricScore`, `validated`, `validationStatus`.

**Body exemplo:**

```json
{
  "service": "SERVICE_DATAVALID_CNH_SERPRO",
  "cpf": "cpf",
  "image1": "{base64Image}"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "biometricScore": 0.98,
    "validated": true,
    "validationStatus": "APPROVED"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Score de inadimplência">

**Service:** `SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP`

**Quando usar:** Use para avaliar risco, score ou propensão associada à pessoa.

**O que retorna:** Retorna score de inadimplencia do CPF, faixa de risco, probabilidade estimada e indicadores usados na avaliacao.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `score`, `riskLevel`, `defaultProbability`.

**Body exemplo:**

```json
{
  "service": "SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "score": 742,
    "riskLevel": "LOW",
    "defaultProbability": "3%"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Score de risco de fraude">

**Service:** `SERVICE_FRAUD_RISK_SCORE_BIGDATACORP`

**Quando usar:** Use para avaliar risco, score ou propensão associada à pessoa.

**O que retorna:** Retorna score de risco de fraude do CPF, fator analisado, nivel de risco, score numerico e sinais que suportam a decisão.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf`, `factor` junto com `service: SERVICE_FRAUD_RISK_SCORE_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `factor`, `score`, `riskLevel`, `indicators`.

**Body exemplo:**

```json
{
  "service": "SERVICE_FRAUD_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf",
  "factor": "minRisk or minattrition"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "factor": "minRisk",
    "score": 720,
    "riskLevel": "LOW",
    "indicators": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Servidores públicos">

**Service:** `SERVICE_PUBLIC_SERVANTS_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Servidores públicos" via API.

**O que retorna:** Retorna registros de servidor publico associados ao CPF, incluindo órgão, cargo, vinculo, remuneracao/faixa e periodo quando disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_PUBLIC_SERVANTS_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `publicServantRecords`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PUBLIC_SERVANTS_BIGDATACORP",
  "cpf": "cpf"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "publicServantRecords": [
      {
        "agency": "Orgao publico",
        "role": "Cargo",
        "status": "ACTIVE"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Status do CPF na Receita Federal">

**Service:** `SERVICE_RFB_PF_BIGDATACORP`

**Quando usar:** Use para consultar ou validar dados cadastrais da pessoa em bases da Receita Federal.

**O que retorna:** Retorna situação do CPF na Receita Federal, incluindo nome, nascimento, status cadastral, comprovante/protocolo e dados fiscais disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_RFB_PF_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `name`, `birthDate`, `registrationStatus`, `protocol`.

**Body exemplo:**

```json
{
  "service": "SERVICE_RFB_PF_BIGDATACORP",
  "cpf": "cpf",
  "dataDeNascimento": "yyyy-MM-dd (opcional)"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "name": "Nome completo",
    "birthDate": "yyyy-MM-dd",
    "registrationStatus": "REGULAR",
    "protocol": "protocolo"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Validação de CPF com endereço">

**Service:** `SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP`

**Quando usar:** Use para consultar ou validar endereços associados ao documento.

**O que retorna:** Retorna se o endereço informado tem associação com o CPF, incluindo nivel de match, endereço normalizado e sinais usados na validação.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf`, `zipcode`, `numberAddress` junto com `service: SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `zipcode`, `match`, `confidence`, `normalizedAddress`.

**Body exemplo:**

```json
{
  "service": "SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP",
  "cpf": "cpf",
  "zipcode": "00000-000",
  "numberAddress": "13"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "zipcode": "01001000",
    "match": true,
    "confidence": "HIGH",
    "normalizedAddress": "Rua Exemplo, 100"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Validação de CPF com telefone">

**Service:** `SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP`

**Quando usar:** Use para consultar, validar ou enriquecer dados de telefone.

**O que retorna:** Retorna se o telefone informado tem associação com o CPF, incluindo nivel de match, tipo de linha, status e sinais de validação.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf`, `phone` junto com `service: SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `phone`, `match`, `confidence`, `lineType`.

**Body exemplo:**

```json
{
  "service": "SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP",
  "cpf": "cpf",
  "phone": "11900000000"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "phone": "11900000000",
    "match": true,
    "confidence": "HIGH",
    "lineType": "MOBILE"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Validação de CPF com telefone (Facetec)">

**Service:** `SERVICE_CPF_PHONE_VALIDATION_FACETEC`

**Quando usar:** Use para consultar, validar ou enriquecer dados de telefone.

**O que retorna:** Retorna validação da associação entre CPF e telefone, com status de match, mensagem da consulta e dados retornados pelo parceiro.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf`, `phone` junto com `service: SERVICE_CPF_PHONE_VALIDATION_FACETEC` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `phone`, `match`, `statusMessage`.

**Body exemplo:**

```json
{
  "service": "SERVICE_CPF_PHONE_VALIDATION_FACETEC",
  "cpf": "cpf",
  "phone": "11900000000"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "phone": "11900000000",
    "match": true,
    "statusMessage": "Telefone associado ao documento"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Validação de e-mail">

**Service:** `SERVICE_EMAIL_VALIDATION_BIGDATACORP`

**Quando usar:** Use para validar ou consultar histórico de e-mails relacionados ao documento.

**O que retorna:** Retorna validação do e-mail informado, incluindo formato, existencia provável, dominio, entregabilidade e indicadores de risco.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `email` junto com `service: SERVICE_EMAIL_VALIDATION_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `email`, `validFormat`, `deliverable`, `domain`, `riskLevel`.

**Body exemplo:**

```json
{
  "service": "SERVICE_EMAIL_VALIDATION_BIGDATACORP",
  "email": "email@email.com"
}
```

**Response resumido:**

```json
{
  "result": {
    "email": "email@email.com",
    "validFormat": true,
    "deliverable": true,
    "domain": "email.com",
    "riskLevel": "LOW"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Validação do E-Social">

**Service:** `SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Validação do E-Social" via API.

**O que retorna:** Retorna qualificação cadastral no eSocial, com status de consistencia entre CPF, NIT/PIS e dados cadastrais informados.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cpf` junto com `service: SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cpf`, `nit`, `qualified`, `inconsistencies`.

**Body exemplo:**

```json
{
  "service": "SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP",
  "cpf": "cpf",
  "nit": "(opcional)"
}
```

**Response resumido:**

```json
{
  "result": {
    "cpf": "cpf",
    "nit": "nit",
    "qualified": true,
    "inconsistencies": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

</AccordionGroup>

### Pessoa Jurídica

<AccordionGroup>
<Accordion title="Certidão negativa de protesto">

**Service:** `SERVICE_PROTEST_PJ_INFOSIMPLES`

**Quando usar:** Use para consultar protestos associados ao documento da empresa.

**O que retorna:** Retorna certidão/consulta de protestos para CNPJ via InfoSimples, com status, cartorios consultados, protestos, valores e datas.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_PROTEST_PJ_INFOSIMPLES` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `hasProtests`, `notaryOffices`, `protests`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PROTEST_PJ_INFOSIMPLES",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "hasProtests": false,
    "notaryOffices": [],
    "protests": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Certidão negativa de protesto PJ (Netrin)">

**Service:** `SERVICE_PROTEST_PJ_NETRIN`

**Quando usar:** Use para consultar protestos associados ao documento da empresa.

**O que retorna:** Retorna certidão/consulta de protestos para CNPJ via Netrin, com status, cartorios consultados, protestos, valores e datas.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_PROTEST_PJ_NETRIN` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `hasProtests`, `notaryOffices`, `protests`.

**Body exemplo:**

```json
{
  "service": "SERVICE_PROTEST_PJ_NETRIN",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "hasProtests": false,
    "notaryOffices": [],
    "protests": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="CNPJ na Receita Federal on-demand">

**Service:** `SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP`

**Quando usar:** Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal.

**O que retorna:** Retorna situação atualizada do CNPJ consultada sob demanda na Receita Federal, com razão social, status cadastral, CNAEs e endereço.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `officialName`, `status`, `openingDate`, `mainActivity`.

**Body exemplo:**

```json
{
  "service": "SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "officialName": "EMPRESA EXEMPLO LTDA",
    "status": "ATIVA",
    "openingDate": "yyyy-MM-dd",
    "mainActivity": "CNAE principal"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Compliance de casas de apostas">

**Service:** `SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP`

**Quando usar:** Use para executar checagens de KYC e compliance da empresa.

**O que retorna:** Retorna indicadores de exposicao da empresa a apostas, bets e compliance regulatório, incluindo sinais de operação, dominio, atividade e alertas.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `hasBettingExposure`, `indicators`, `riskLevel`.

**Body exemplo:**

```json
{
  "service": "SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "hasBettingExposure": true,
    "indicators": [
      "atividade relacionada"
    ],
    "riskLevel": "MEDIUM"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="DAS MEI na Receita">

**Service:** `SERVICE_DAS_MEI_INFOSIMPLES`

**Quando usar:** Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal.

**O que retorna:** Retorna informações de DAS MEI e situação fiscal relacionada ao CNPJ, incluindo periodos, pagamentos, pendencias e status quando disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_DAS_MEI_INFOSIMPLES` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `meiStatus`, `periods`.

**Body exemplo:**

```json
{
  "service": "SERVICE_DAS_MEI_INFOSIMPLES",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "meiStatus": "ACTIVE",
    "periods": [
      {
        "period": "2026-01",
        "paid": true
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Débitos ativos">

**Service:** `SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP`

**Quando usar:** Use para consultar débitos ou dívidas associadas à empresa.

**O que retorna:** Retorna dívidas ativas vinculadas ao CNPJ, com origem do débito, valores, situação, órgão credor e status da consulta.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `totalDebts`, `totalValue`, `debts`.

**Body exemplo:**

```json
{
  "service": "SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "totalDebts": 1,
    "totalValue": "9800.00",
    "debts": [
      {
        "source": "PGFN",
        "value": "9800.00",
        "status": "ACTIVE"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Doações eleitorais">

**Service:** `SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP`

**Quando usar:** Use para consultar informações eleitorais relacionadas à empresa.

**O que retorna:** Retorna doações eleitorais realizadas pela empresa, com ano, candidato/partido, valor, cargo, UF e detalhes da prestacao de contas.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `donations`.

**Body exemplo:**

```json
{
  "service": "SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "donations": [
      {
        "year": 2024,
        "recipient": "Candidato",
        "amount": "1000.00"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Doações eleitorais dos sócios">

**Service:** `SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP`

**Quando usar:** Use para consultar informações eleitorais relacionadas à empresa.

**O que retorna:** Retorna doações eleitorais feitas pelos sócios da empresa, com sócio relacionado, ano, candidato/partido, valor e detalhes eleitorais.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `ownersDonations`.

**Body exemplo:**

```json
{
  "service": "SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "ownersDonations": [
      {
        "ownerName": "Nome do sócio",
        "year": 2024,
        "recipient": "Candidato",
        "amount": "300.00"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Endereços estendidos">

**Service:** `SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP`

**Quando usar:** Use para consultar ou validar endereços associados ao documento.

**O que retorna:** Retorna endereços associados ao CNPJ, incluindo logradouro, bairro, cidade, UF, CEP, pais, tipo de endereço e dados complementares quando disponíveis.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `totalAddresses`, `addresses`.

**Body exemplo:**

```json
{
  "service": "SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "totalAddresses": 1,
    "addresses": [
      {
        "address": "Av Exemplo",
        "city": "Sao Paulo",
        "state": "SP",
        "zipcode": "01001000",
        "addressType": "COMMERCIAL"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Enriquecimento de dados">

**Service:** `SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP`

**Quando usar:** Use para complementar dados cadastrais da empresa a partir do documento informado.

**O que retorna:** Retorna cadastro completo da empresa, incluindo razão social, nome fantasia, situação cadastral, CNAEs, natureza juridica, porte, capital e endereço.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `officialName`, `tradeName`, `status`, `mainActivity`.

**Body exemplo:**

```json
{
  "service": "SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "officialName": "EMPRESA EXEMPLO LTDA",
    "tradeName": "EMPRESA EXEMPLO",
    "status": "ATIVA",
    "mainActivity": "CNAE principal"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Exposição e perfil na mídia dos sócios">

**Service:** `SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP`

**Quando usar:** Use para consultar vínculos, sócios ou relacionamentos associados à empresa.

**O que retorna:** Retorna exposicao e perfil de mídia da empresa e sócios, com noticias, fontes, categorias, sentimento, relevancia e alertas encontrados.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `mediaMentions`, `exposureLevel`.

**Body exemplo:**

```json
{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "mediaMentions": [
      {
        "title": "Noticia encontrada",
        "source": "Fonte",
        "sentiment": "NEUTRAL"
      }
    ],
    "exposureLevel": "LOW"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Fornecedores eleitorais">

**Service:** `SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP`

**Quando usar:** Use para consultar informações eleitorais relacionadas à empresa.

**O que retorna:** Retorna prestações de serviço eleitorais vinculadas ao CNPJ, com campanha, candidato/partido, valor, ano e natureza do serviço.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `providers`.

**Body exemplo:**

```json
{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "providers": [
      {
        "year": 2024,
        "campaign": "Campanha",
        "amount": "2500.00",
        "serviceType": "Servico"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="KYC e compliance dos sócios">

**Service:** `SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP`

**Quando usar:** Use para executar checagens de KYC e compliance da empresa.

**O que retorna:** Retorna checagens de KYC e compliance dos sócios da empresa, incluindo PEP, sancoes, mídia, risco e alertas encontrados por sócio.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `ownersChecked`, `owners`.

**Body exemplo:**

```json
{
  "service": "SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "ownersChecked": 2,
    "owners": [
      {
        "name": "Nome do sócio",
        "isPep": false,
        "sanctions": [],
        "riskAlerts": []
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Processos jurídicos dos sócios">

**Service:** `SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP`

**Quando usar:** Use para consultar certidões, processos ou informações jurídicas da empresa.

**O que retorna:** Retorna processos jurídicos associados aos sócios da empresa, com sócio relacionado, tribunal, classe, assunto, status e datas.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `ownersProcesses`.

**Body exemplo:**

```json
{
  "service": "SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "ownersProcesses": [
      {
        "ownerName": "Nome do sócio",
        "totalProcesses": 1,
        "processes": []
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Relacionamentos da empresa">

**Service:** `SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP`

**Quando usar:** Use para consultar vínculos, sócios ou relacionamentos associados à empresa.

**O que retorna:** Retorna relacionamentos da empresa, como sócios, proprietarios, empresas relacionadas, participacoes e vínculos societários identificados.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `owners`, `relatedCompanies`.

**Body exemplo:**

```json
{
  "service": "SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "owners": [
      {
        "name": "Nome do sócio",
        "document": "cpf",
        "share": "50%"
      }
    ],
    "relatedCompanies": []
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="SINTEGRA">

**Service:** `SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "SINTEGRA" via API.

**O que retorna:** Retorna dados do SINTEGRA, incluindo inscrição estadual, UF, situação, regime, atividades, endereço e mensagens da consulta.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `stateRegistration`, `state`, `status`, `regime`.

**Body exemplo:**

```json
{
  "service": "SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP",
  "cnpj": "cnpj",
  "uf": "uf (opcional)"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "stateRegistration": "000000000",
    "state": "SP",
    "status": "HABILITADO",
    "regime": "NORMAL"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Sócios de primeiro nível">

**Service:** `SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP`

**Quando usar:** Use para consultar vínculos, sócios ou relacionamentos associados à empresa.

**O que retorna:** Retorna sócios de primeiro nivel da empresa, com nome, documento, participação, qualificação e vínculos diretos ao CNPJ.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `partners`.

**Body exemplo:**

```json
{
  "service": "SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "partners": [
      {
        "name": "Nome do sócio",
        "document": "cpf",
        "level": 1,
        "qualification": "SOCIO"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Sócios na Receita Federal">

**Service:** `SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP`

**Quando usar:** Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal.

**O que retorna:** Retorna o quadro societario na Receita Federal, com nome dos sócios, documentos mascarados, qualificação, participação e data de entrada quando disponível.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `owners`.

**Body exemplo:**

```json
{
  "service": "SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "owners": [
      {
        "name": "Nome do sócio",
        "qualification": "SOCIO-ADMINISTRADOR",
        "entryDate": "yyyy-MM-dd"
      }
    ]
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

<Accordion title="Status do CNPJ na Receita Federal">

**Service:** `SERVICE_RFB_PJ_BIGDATACORP`

**Quando usar:** Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal.

**O que retorna:** Retorna situação do CNPJ na Receita Federal, incluindo razão social, nome fantasia, situação cadastral, abertura, CNAEs e endereço.

**Passo a passo:**

1. Gere um token em `POST /api/token-generate`.
2. Envie `cnpj` junto com `service: SERVICE_RFB_PJ_BIGDATACORP` em `POST /api/service-api`.
3. Confira `status.code` e `status.message` para saber se a consulta processou.
4. Leia `result`, que neste service costuma trazer: `cnpj`, `officialName`, `status`, `openingDate`, `mainActivity`.

**Body exemplo:**

```json
{
  "service": "SERVICE_RFB_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

**Response resumido:**

```json
{
  "result": {
    "cnpj": "cnpj",
    "officialName": "EMPRESA EXEMPLO LTDA",
    "status": "ATIVA",
    "openingDate": "yyyy-MM-dd",
    "mainActivity": "CNAE principal"
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

</Accordion>

</AccordionGroup>