---
title: Exemplos por ambiente
description: Copie chamadas equivalentes para homologação e produção sem trocar parâmetros por engano
---

# Exemplos por ambiente

Use esta página quando precisar conferir rapidamente a mesma chamada em
homologação e produção. A estrutura do request é a mesma; o que muda é a URL
base.

<Warning>
Use credenciais, token e base URL do mesmo ambiente. Token de homologação não
deve ser usado em produção, e token de produção não deve ser usado em
homologação.
</Warning>

## URLs base

| Ambiente | Base URL | Quando usar |
| --- | --- | --- |
| Homologação | `https://backoffice-hml.idcerberus.com` | Testes, validação de payload e homologação da integração |
| Produção | `https://backoffice.idcerberus.com` | Operação real, com credenciais produtivas |

## Gerar token

<Tabs>
<Tab title="Homologação">

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/token-generate' \
--header 'Content-Type: application/json' \
--data '{
  "client": "{client}",
  "secret": "{secret}"
}'
```

</Tab>
<Tab title="Produção">

```bash
curl --location 'https://backoffice.idcerberus.com/api/token-generate' \
--header 'Content-Type: application/json' \
--data '{
  "client": "{client}",
  "secret": "{secret}"
}'
```

</Tab>
</Tabs>

## Consulta de CPF

<Tabs>
<Tab title="Homologação">

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PF_BIGDATACORP",
  "cpf": "cpf",
  "dataDeNascimento": "yyyy-MM-dd (opcional)"
}'
```

</Tab>
<Tab title="Produção">

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PF_BIGDATACORP",
  "cpf": "cpf",
  "dataDeNascimento": "yyyy-MM-dd (opcional)"
}'
```

</Tab>
</Tabs>

## Consulta de CNPJ

<Tabs>
<Tab title="Homologação">

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

</Tab>
<Tab title="Produção">

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

</Tab>
</Tabs>

## Arquivos prontos

Alguns exemplos também são publicados como arquivos `.curl`:

| Arquivo | Uso |
| --- | --- |
| [`auth.hml.curl`](https://react-it.github.io/api-docs-idcerberus/examples/auth.hml.curl) | Gerar token em homologação |
| [`auth.prod.curl`](https://react-it.github.io/api-docs-idcerberus/examples/auth.prod.curl) | Gerar token em produção |
| [`service-api-cpf.hml.curl`](https://react-it.github.io/api-docs-idcerberus/examples/service-api-cpf.hml.curl) | Consultar CPF em homologação |
| [`service-api-cpf.prod.curl`](https://react-it.github.io/api-docs-idcerberus/examples/service-api-cpf.prod.curl) | Consultar CPF em produção |
| [`service-api-cnpj.hml.curl`](https://react-it.github.io/api-docs-idcerberus/examples/service-api-cnpj.hml.curl) | Consultar CNPJ em homologação |
| [`service-api-cnpj.prod.curl`](https://react-it.github.io/api-docs-idcerberus/examples/service-api-cnpj.prod.curl) | Consultar CNPJ em produção |
| [`facematch.hml.curl`](https://react-it.github.io/api-docs-idcerberus/examples/facematch.hml.curl) | Executar FaceMatch em homologação |
| [`documentoscopia.hml.curl`](https://react-it.github.io/api-docs-idcerberus/examples/documentoscopia.hml.curl) | Executar documentoscopia em homologação |
