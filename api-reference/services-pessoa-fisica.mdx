---
title: Services de pessoa física
description: Catálogo explícito dos services de pessoa física disponíveis via API, com campos esperados e exemplos de request.
---

# Services de pessoa física

Catálogo explícito dos services de pessoa física disponíveis via API, com campos esperados e exemplos de request.

<Info>
Todos os services abaixo usam o mesmo endpoint: `POST /api/service-api`. O produto executado é definido pelo campo `service` no body da requisição.
</Info>

## Como ler esta referência

- **Nome**: nome funcional do produto.
- **Service**: valor exato que deve ser enviado no campo `service`.
- **Família**: agrupamento por objetivo de uso, como dados cadastrais, risco, jurídico ou biometria.
- **Campos**: parâmetros esperados no body além de `service`.
- **Retorno principal**: resumo dos dados esperados no objeto `result` para aquele service.
- **Exemplos**: cada item traz body JSON, curl de homologação, curl de produção e response resumido.

## Índice por família

### Biometria e documentos

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Documentoscopia digital | `SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX` | `key`, `image1`, `image2`, `selfie1` | Use para avaliar documento, selfie e biometria dentro do fluxo de documentoscopia. | Retorna status da analise documental, chave da consulta e dados processados de documento, selfie e validacoes associadas. |
| FaceMatch | `SERVICE_FACE_MATCH_BIGDATACORP` | `image1`, `image2` | Use para comparar duas imagens faciais e retornar a similaridade entre elas. | Retorna status da comparacao facial, percentual/score de similaridade e indicadores usados para aprovar ou reprovar a comparacao. |
| FaceMatch (AWS) | `SERVICE_FACE_MATCH_AWS` | `image1`, `image2` | Use para comparar duas imagens faciais e retornar a similaridade entre elas. | Retorna status da comparacao facial, percentual/score de similaridade e indicadores usados para aprovar ou reprovar a comparacao. |
| Liveness 2D | `SERVICE_LIVENESS_2D_FACETEC` | `image1` | Use para validar prova de vida a partir de uma imagem de selfie. | Retorna status da comparacao facial, percentual/score de similaridade e indicadores usados para aprovar ou reprovar a comparacao. |
| OCR de documentos | `SERVICE_OCR_BIGDATACORP` | `image1`, `image2`, `image1Url`, `image2Url` | Use para extrair dados de documentos enviados em base64 ou por URL. | Retorna dados extraidos do documento enviado, tipo documental identificado, campos oficiais/estimados e status de leitura. |
| Resultado da documentoscopia digital | `SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX` | `key` | Use para avaliar documento, selfie e biometria dentro do fluxo de documentoscopia. | Retorna status da analise documental, chave da consulta e dados processados de documento, selfie e validacoes associadas. |
| Score biométrico | `SERVICE_DATAVALID_CNH_SERPRO` | `cpf`, `image1` | Use para comparar a imagem enviada com bases biométricas disponíveis e retornar a similaridade. | Retorna score/status biometrico e dados de validacao conforme a base governamental consultada. |

### Contatos, sites e relacionamentos

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Dados financeiros e endereços | `SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP` | `cpf`, `birthDate` | Use para consultar informações financeiras associadas à pessoa. | Retorna indicadores financeiros e de risco do pessoa/CPF, como scores, debitos, renda/ativos estimados e sinais de inadimplencia quando aplicavel. |
| Dados pelo telefone | `SERVICE_CONFIRM_PHONE_FACETEC` | `phone` | Use para consultar, validar ou enriquecer dados de telefone. | Retorna status da comparacao facial, percentual/score de similaridade e indicadores usados para aprovar ou reprovar a comparacao. |
| Endereços | `SERVICE_ADDRESS_BIGDATACORP` | `cpf` | Use para consultar ou validar endereços associados ao documento. | Retorna enderecos encontrados ou resultado de validacao de endereco associado ao pessoa/CPF. |
| Histórico de e-mails (BigDataCorp) | `SERVICE_EMAILS_EXTENDED_BIGDATACORP` | `cpf` | Use para validar ou consultar histórico de e-mails relacionados ao documento. | Retorna e-mails, historico de contato ou resultado de validacao de e-mail associado ao pessoa/CPF. |
| Histórico de telefones (BigDataCorp) | `SERVICE_PHONE_HISTORY_BIGDATACORP` | `cpf` | Use para consultar, validar ou enriquecer dados de telefone. | Retorna telefones, historico de contato ou resultado de validacao de telefone associado ao pessoa/CPF. |
| Pessoas relacionadas (BigDataCorp) | `SERVICE_RELATED_PEOPLE_BIGDATACORP` | `cpf` | Use para consultar vínculos, sócios ou relacionamentos associados à pessoa. | Retorna vinculos, socios, relacionamentos economicos ou pessoas/empresas relacionadas ao pessoa/CPF. |
| Relacionamentos econômicos | `SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP` | `cpf` | Use para consultar vínculos, sócios ou relacionamentos associados à pessoa. | Retorna vinculos, socios, relacionamentos economicos ou pessoas/empresas relacionadas ao pessoa/CPF. |
| Validação de CPF com endereço | `SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP` | `cpf`, `zipcode`, `numberAddress` | Use para consultar ou validar endereços associados ao documento. | Retorna enderecos encontrados ou resultado de validacao de endereco associado ao pessoa/CPF. |
| Validação de CPF com telefone | `SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP` | `cpf`, `phone` | Use para consultar, validar ou enriquecer dados de telefone. | Retorna telefones, historico de contato ou resultado de validacao de telefone associado ao pessoa/CPF. |
| Validação de CPF com telefone (Facetec) | `SERVICE_CPF_PHONE_VALIDATION_FACETEC` | `cpf`, `phone` | Use para consultar, validar ou enriquecer dados de telefone. | Retorna status da comparacao facial, percentual/score de similaridade e indicadores usados para aprovar ou reprovar a comparacao. |
| Validação de e-mail | `SERVICE_EMAIL_VALIDATION_BIGDATACORP` | `email` | Use para validar ou consultar histórico de e-mails relacionados ao documento. | Retorna e-mails, historico de contato ou resultado de validacao de e-mail associado ao pessoa/CPF. |

### Dados cadastrais e Receita Federal

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Consulta de MEI | `SERVICE_MEI_BIGDATACORP` | `cpf` | Use este service quando precisar executar a consulta "Consulta de MEI" via API. | Retorna dados cadastrais ou fiscais especificos do pessoa/CPF, conforme a base consultada pelo service. |
| CPF na Receita Federal on-demand | `SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP` | `cpf` | Use para consultar ou validar dados cadastrais da pessoa em bases da Receita Federal. | Retorna dados cadastrais do pessoa/CPF, incluindo status cadastral, identificacao, datas e atributos disponiveis na base consultada. |
| Dados PIS | `SERVICE_PIS_CONSULTATION_BIGDATACORP` | `cpf` | Use este service quando precisar executar a consulta "Dados PIS" via API. | Retorna dados cadastrais ou fiscais especificos do pessoa/CPF, conforme a base consultada pelo service. |
| Enriquecimento de dados | `SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP` | `cpf` | Use para complementar dados cadastrais da pessoa a partir do documento informado. | Retorna dados cadastrais do pessoa/CPF, incluindo status cadastral, identificacao, datas e atributos disponiveis na base consultada. |
| Status do CPF na Receita Federal | `SERVICE_RFB_PF_BIGDATACORP` | `cpf`, `dataDeNascimento` | Use para consultar ou validar dados cadastrais da pessoa em bases da Receita Federal. | Retorna dados cadastrais do pessoa/CPF, incluindo status cadastral, identificacao, datas e atributos disponiveis na base consultada. |
| Validação do E-Social | `SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP` | `cpf`, `nit` | Use este service quando precisar executar a consulta "Validação do E-Social" via API. | Retorna dados cadastrais do pessoa/CPF, incluindo status cadastral, identificacao, datas e atributos disponiveis na base consultada. |

### Dados eleitorais e PEP

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Dados eleitorais de candidato | `SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP` | `cpf` | Use para consultar informações eleitorais relacionadas à pessoa. | Retorna dados eleitorais associados ao pessoa/CPF, como candidaturas, doacoes, fornecedores ou historico politico quando disponivel. |
| Doações eleitorais PF (BigDataCorp) | `SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP` | `cpf` | Use para consultar informações eleitorais relacionadas à pessoa. | Retorna dados eleitorais associados ao pessoa/CPF, como candidaturas, doacoes, fornecedores ou historico politico quando disponivel. |
| Envolvimento político (BigDataCorp) | `SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP` | `cpf` | Use este service quando precisar executar a consulta "Envolvimento político (BigDataCorp)" via API. | Retorna indicadores de KYC/compliance do pessoa/CPF, como PEP, sancoes, exposicao, historicos e sinais de risco quando disponiveis. |
| Envolvimento político PF (BigDataCorp) | `SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP` | `cpf` | Use este service quando precisar executar a consulta "Envolvimento político PF (BigDataCorp)" via API. | Retorna indicadores de KYC/compliance do pessoa/CPF, como PEP, sancoes, exposicao, historicos e sinais de risco quando disponiveis. |
| Histórico político familiar PF (BigDataCorp) | `SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP` | `cpf` | Use este service quando precisar executar a consulta "Histórico político familiar PF (BigDataCorp)" via API. | Retorna indicadores de KYC/compliance do pessoa/CPF, como PEP, sancoes, exposicao, historicos e sinais de risco quando disponiveis. |
| Pessoa politicamente exposta | `SERVICE_PEP` | `cpf` | Use para verificar exposição política ou vínculo com Pessoa Politicamente Exposta. | Retorna indicadores de KYC/compliance do pessoa/CPF, como PEP, sancoes, exposicao, historicos e sinais de risco quando disponiveis. |
| Prestadores de serviços eleitorais PF (BigDataCorp) | `SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP` | `cpf` | Use para consultar informações eleitorais relacionadas à pessoa. | Retorna dados eleitorais associados ao pessoa/CPF, como candidaturas, doacoes, fornecedores ou historico politico quando disponivel. |

### Jurídico, certidões e protestos

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Antecedentes criminais civis | `SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP` | `cpf`, `rg`, `uf` | Use para consultar certidões, processos ou informações jurídicas da pessoa. | Retorna ocorrencias juridicas, certidoes, protestos, antecedentes ou mandados associados ao pessoa/CPF, alem do status da consulta. |
| Antecedentes criminais federais | `SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP` | `cpf` | Use para consultar certidões, processos ou informações jurídicas da pessoa. | Retorna ocorrencias juridicas, certidoes, protestos, antecedentes ou mandados associados ao pessoa/CPF, alem do status da consulta. |
| Certidão de Nada Consta | `SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP` | `cpf`, `court`, `uf`, `sphere` | Use para consultar certidões, processos ou informações jurídicas da pessoa. | Retorna ocorrencias juridicas, certidoes, protestos, antecedentes ou mandados associados ao pessoa/CPF, alem do status da consulta. |
| Certidão negativa de protesto | `SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP` | `cpf` | Use para consultar protestos associados ao documento da pessoa. | Retorna ocorrencias juridicas, certidoes, protestos, antecedentes ou mandados associados ao pessoa/CPF, alem do status da consulta. |
| Certidão negativa de protesto PF (InfoSimples) | `SERVICE_PROTEST_PF_INFOSIMPLES` | `cpf` | Use para consultar protestos associados ao documento da pessoa. | Retorna ocorrencias juridicas, certidoes, protestos, antecedentes ou mandados associados ao pessoa/CPF, alem do status da consulta. |
| Certidão negativa de protesto PF (Netrin) | `SERVICE_PROTEST_PF_NETRIN` | `cpf` | Use para consultar protestos associados ao documento da pessoa. | Retorna ocorrencias juridicas, certidoes, protestos, antecedentes ou mandados associados ao pessoa/CPF, alem do status da consulta. |
| Mandado de prisão | `SERVICE_ARREST_WARRANT` | `nome`, `motherName`, `fatherName`, `birthDate`, `cpf` | Use este service quando precisar executar a consulta "Mandado de prisão" via API. | Retorna ocorrencias juridicas, certidoes, protestos, antecedentes ou mandados associados ao pessoa/CPF, alem do status da consulta. |
| Processos jurídicos e administrativos | `SERVICE_JURIDICAL_PROCESSES_BIGDATACORP` | `cpf` | Use para consultar certidões, processos ou informações jurídicas da pessoa. | Retorna ocorrencias juridicas, certidoes, protestos, antecedentes ou mandados associados ao pessoa/CPF, alem do status da consulta. |

### KYC, compliance e exposição

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Exposição e perfil na mídia PF (BigDataCorp) | `SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP` | `cpf` | Use este service quando precisar executar a consulta "Exposição e perfil na mídia PF (BigDataCorp)" via API. | Retorna o objeto result do service SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP com os dados disponiveis para a consulta, alem do status de processamento. |
| KYC e compliance | `SERVICE_PERSON_KYC_BIGDATACORP` | `cpf`, `birthDate` | Use para executar checagens de KYC e compliance da pessoa. | Retorna indicadores de KYC/compliance do pessoa/CPF, como PEP, sancoes, exposicao, historicos e sinais de risco quando disponiveis. |
| Propensão a apostas online | `SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP` | `cpf` | Use este service quando precisar executar a consulta "Propensão a apostas online" via API. | Retorna o objeto result do service SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP com os dados disponiveis para a consulta, alem do status de processamento. |

### Outros services

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Histórico profissional | `SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP` | `cpf` | Use este service quando precisar executar a consulta "Histórico profissional" via API. | Retorna o objeto result do service SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP com os dados disponiveis para a consulta, alem do status de processamento. |
| Histórico profissional do titular | `SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP` | `cpf`, `birthDate` | Use este service quando precisar executar a consulta "Histórico profissional do titular" via API. | Retorna vinculos, socios, relacionamentos economicos ou pessoas/empresas relacionadas ao pessoa/CPF. |
| Modelagem de dados | `SERVICE_PERSON_DATA_MODELING_BIGDATACORP` | `cpf` | Use este service quando precisar executar a consulta "Modelagem de dados" via API. | Retorna o objeto result do service SERVICE_PERSON_DATA_MODELING_BIGDATACORP com os dados disponiveis para a consulta, alem do status de processamento. |
| Prompt de IA para pessoa | `SERVICE_PERSON_AI_PROMPT_OPENAI` | `cpf` | Use este service quando precisar executar a consulta "Prompt de IA para pessoa" via API. | Retorna a resposta consolidada pela IA a partir dos dados consultados e do prompt configurado para o service. |
| Servidores públicos | `SERVICE_PUBLIC_SERVANTS_BIGDATACORP` | `cpf` | Use este service quando precisar executar a consulta "Servidores públicos" via API. | Retorna o objeto result do service SERVICE_PUBLIC_SERVANTS_BIGDATACORP com os dados disponiveis para a consulta, alem do status de processamento. |

### Risco, crédito e dívidas

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Dívida ativa | `SERVICE_ACTIVE_DEBT_PF_BIGDATACORP` | `cpf` | Use para consultar débitos ou dívidas associadas à pessoa. | Retorna indicadores financeiros e de risco do pessoa/CPF, como scores, debitos, renda/ativos estimados e sinais de inadimplencia quando aplicavel. |
| Informações financeiras | `SERVICE_FINANCIAL_INFORMATION_BIGDATACORP` | `cpf` | Use para consultar informações financeiras associadas à pessoa. | Retorna indicadores financeiros e de risco do pessoa/CPF, como scores, debitos, renda/ativos estimados e sinais de inadimplencia quando aplicavel. |
| Risco financeiro | `SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP` | `cpf`, `birthDate` | Use para consultar informações financeiras associadas à pessoa. | Retorna indicadores financeiros e de risco do pessoa/CPF, como scores, debitos, renda/ativos estimados e sinais de inadimplencia quando aplicavel. |
| Score de inadimplência | `SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP` | `cpf` | Use para avaliar risco, score ou propensão associada à pessoa. | Retorna indicadores financeiros e de risco do pessoa/CPF, como scores, debitos, renda/ativos estimados e sinais de inadimplencia quando aplicavel. |
| Score de risco de fraude | `SERVICE_FRAUD_RISK_SCORE_BIGDATACORP` | `cpf`, `factor` | Use para avaliar risco, score ou propensão associada à pessoa. | Retorna indicadores financeiros e de risco do pessoa/CPF, como scores, debitos, renda/ativos estimados e sinais de inadimplencia quando aplicavel. |


## Requests completos

### Biometria e documentos

<AccordionGroup>
<Accordion title="Documentoscopia digital">

**Service:** `SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX`

**Quando usar:** Use para avaliar documento, selfie e biometria dentro do fluxo de documentoscopia.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `key`, `image1`, `image2`, `selfie1`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX",
  "key": "84bfcd2e-2336-4e30-bcab-15348b7890b5",
  "image1": "base64",
  "image2": "base64",
  "selfie1": "base64"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX",
  "key": "84bfcd2e-2336-4e30-bcab-15348b7890b5",
  "image1": "base64",
  "image2": "base64",
  "selfie1": "base64"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX",
  "key": "84bfcd2e-2336-4e30-bcab-15348b7890b5",
  "image1": "base64",
  "image2": "base64",
  "selfie1": "base64"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `key` | Sim | Parâmetro usado pelo service SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX. |
| `image1` | Sim | Parâmetro usado pelo service SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX. |
| `image2` | Sim | Parâmetro usado pelo service SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX. |
| `selfie1` | Sim | Parâmetro usado pelo service SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="FaceMatch">

**Service:** `SERVICE_FACE_MATCH_BIGDATACORP`

**Quando usar:** Use para comparar duas imagens faciais e retornar a similaridade entre elas.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `image1`, `image2`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_FACE_MATCH_BIGDATACORP",
  "image1": "base64",
  "image2": "base64"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FACE_MATCH_BIGDATACORP",
  "image1": "base64",
  "image2": "base64"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FACE_MATCH_BIGDATACORP",
  "image1": "base64",
  "image2": "base64"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `image1` | Sim | Parâmetro usado pelo service SERVICE_FACE_MATCH_BIGDATACORP. |
| `image2` | Sim | Parâmetro usado pelo service SERVICE_FACE_MATCH_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_FACE_MATCH_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="FaceMatch (AWS)">

**Service:** `SERVICE_FACE_MATCH_AWS`

**Quando usar:** Use para comparar duas imagens faciais e retornar a similaridade entre elas.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `image1`, `image2`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_FACE_MATCH_AWS",
  "image1": "base64",
  "image2": "base64"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FACE_MATCH_AWS",
  "image1": "base64",
  "image2": "base64"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FACE_MATCH_AWS",
  "image1": "base64",
  "image2": "base64"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `image1` | Sim | Parâmetro usado pelo service SERVICE_FACE_MATCH_AWS. |
| `image2` | Sim | Parâmetro usado pelo service SERVICE_FACE_MATCH_AWS. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_FACE_MATCH_AWS."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Liveness 2D">

**Service:** `SERVICE_LIVENESS_2D_FACETEC`

**Quando usar:** Use para validar prova de vida a partir de uma imagem de selfie.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `image1`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_LIVENESS_2D_FACETEC",
  "image1": "selfie"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_LIVENESS_2D_FACETEC",
  "image1": "selfie"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_LIVENESS_2D_FACETEC",
  "image1": "selfie"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `image1` | Sim | Parâmetro usado pelo service SERVICE_LIVENESS_2D_FACETEC. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_LIVENESS_2D_FACETEC."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="OCR de documentos">

**Service:** `SERVICE_OCR_BIGDATACORP`

**Quando usar:** Use para extrair dados de documentos enviados em base64 ou por URL.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `image1`, `image2`, `image1Url`, `image2Url`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_OCR_BIGDATACORP",
  "image1": "base64",
  "image2": "base64",
  "image1Url": "url_image",
  "image2Url": "urlImageMatch"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OCR_BIGDATACORP",
  "image1": "base64",
  "image2": "base64",
  "image1Url": "url_image",
  "image2Url": "urlImageMatch"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OCR_BIGDATACORP",
  "image1": "base64",
  "image2": "base64",
  "image1Url": "url_image",
  "image2Url": "urlImageMatch"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `image1` | Sim | Parâmetro usado pelo service SERVICE_OCR_BIGDATACORP. |
| `image2` | Sim | Parâmetro usado pelo service SERVICE_OCR_BIGDATACORP. |
| `image1Url` | Sim | Parâmetro usado pelo service SERVICE_OCR_BIGDATACORP. |
| `image2Url` | Sim | Parâmetro usado pelo service SERVICE_OCR_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_OCR_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Resultado da documentoscopia digital">

**Service:** `SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX`

**Quando usar:** Use para avaliar documento, selfie e biometria dentro do fluxo de documentoscopia.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `key`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX",
  "key": "de0cd562-5962-40bd-8f94-5a7184ecde0e"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX",
  "key": "de0cd562-5962-40bd-8f94-5a7184ecde0e"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX",
  "key": "de0cd562-5962-40bd-8f94-5a7184ecde0e"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `key` | Sim | Parâmetro usado pelo service SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Score biométrico">

**Service:** `SERVICE_DATAVALID_CNH_SERPRO`

**Quando usar:** Use para comparar a imagem enviada com bases biométricas disponíveis e retornar a similaridade.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`, `image1`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_DATAVALID_CNH_SERPRO",
  "cpf": "cpf",
  "image1": "{base64Image}"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DATAVALID_CNH_SERPRO",
  "cpf": "cpf",
  "image1": "{base64Image}"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DATAVALID_CNH_SERPRO",
  "cpf": "cpf",
  "image1": "{base64Image}"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_DATAVALID_CNH_SERPRO. |
| `image1` | Sim | Parâmetro usado pelo service SERVICE_DATAVALID_CNH_SERPRO. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_DATAVALID_CNH_SERPRO."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Contatos, sites e relacionamentos

<AccordionGroup>
<Accordion title="Dados financeiros e endereços">

**Service:** `SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP`

**Quando usar:** Use para consultar informações financeiras associadas à pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** `birthDate`

### Body

```json
{
  "service": "SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP. |
| `birthDate` | Não | Parâmetro usado pelo service SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Dados pelo telefone">

**Service:** `SERVICE_CONFIRM_PHONE_FACETEC`

**Quando usar:** Use para consultar, validar ou enriquecer dados de telefone.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `phone`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_CONFIRM_PHONE_FACETEC",
  "phone": "+5561123456789"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CONFIRM_PHONE_FACETEC",
  "phone": "+5561123456789"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CONFIRM_PHONE_FACETEC",
  "phone": "+5561123456789"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `phone` | Sim | Parâmetro usado pelo service SERVICE_CONFIRM_PHONE_FACETEC. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_CONFIRM_PHONE_FACETEC."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Endereços">

**Service:** `SERVICE_ADDRESS_BIGDATACORP`

**Quando usar:** Use para consultar ou validar endereços associados ao documento.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ADDRESS_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ADDRESS_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ADDRESS_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_ADDRESS_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ADDRESS_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Histórico de e-mails (BigDataCorp)">

**Service:** `SERVICE_EMAILS_EXTENDED_BIGDATACORP`

**Quando usar:** Use para validar ou consultar histórico de e-mails relacionados ao documento.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_EMAILS_EXTENDED_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_EMAILS_EXTENDED_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_EMAILS_EXTENDED_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_EMAILS_EXTENDED_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_EMAILS_EXTENDED_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Histórico de telefones (BigDataCorp)">

**Service:** `SERVICE_PHONE_HISTORY_BIGDATACORP`

**Quando usar:** Use para consultar, validar ou enriquecer dados de telefone.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PHONE_HISTORY_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PHONE_HISTORY_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PHONE_HISTORY_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PHONE_HISTORY_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PHONE_HISTORY_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Pessoas relacionadas (BigDataCorp)">

**Service:** `SERVICE_RELATED_PEOPLE_BIGDATACORP`

**Quando usar:** Use para consultar vínculos, sócios ou relacionamentos associados à pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_RELATED_PEOPLE_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RELATED_PEOPLE_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RELATED_PEOPLE_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_RELATED_PEOPLE_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_RELATED_PEOPLE_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Relacionamentos econômicos">

**Service:** `SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP`

**Quando usar:** Use para consultar vínculos, sócios ou relacionamentos associados à pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Validação de CPF com endereço">

**Service:** `SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP`

**Quando usar:** Use para consultar ou validar endereços associados ao documento.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`, `zipcode`, `numberAddress`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP",
  "cpf": "cpf",
  "zipcode": "00000-000",
  "numberAddress": "13"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP",
  "cpf": "cpf",
  "zipcode": "00000-000",
  "numberAddress": "13"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP",
  "cpf": "cpf",
  "zipcode": "00000-000",
  "numberAddress": "13"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP. |
| `zipcode` | Sim | Parâmetro usado pelo service SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP. |
| `numberAddress` | Sim | Parâmetro usado pelo service SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Validação de CPF com telefone">

**Service:** `SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP`

**Quando usar:** Use para consultar, validar ou enriquecer dados de telefone.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`, `phone`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP",
  "cpf": "cpf",
  "phone": "11900000000"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP",
  "cpf": "cpf",
  "phone": "11900000000"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP",
  "cpf": "cpf",
  "phone": "11900000000"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP. |
| `phone` | Sim | Parâmetro usado pelo service SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Validação de CPF com telefone (Facetec)">

**Service:** `SERVICE_CPF_PHONE_VALIDATION_FACETEC`

**Quando usar:** Use para consultar, validar ou enriquecer dados de telefone.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`, `phone`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_CPF_PHONE_VALIDATION_FACETEC",
  "cpf": "cpf",
  "phone": "11900000000"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CPF_PHONE_VALIDATION_FACETEC",
  "cpf": "cpf",
  "phone": "11900000000"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CPF_PHONE_VALIDATION_FACETEC",
  "cpf": "cpf",
  "phone": "11900000000"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_CPF_PHONE_VALIDATION_FACETEC. |
| `phone` | Sim | Parâmetro usado pelo service SERVICE_CPF_PHONE_VALIDATION_FACETEC. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_CPF_PHONE_VALIDATION_FACETEC."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Validação de e-mail">

**Service:** `SERVICE_EMAIL_VALIDATION_BIGDATACORP`

**Quando usar:** Use para validar ou consultar histórico de e-mails relacionados ao documento.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `email`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_EMAIL_VALIDATION_BIGDATACORP",
  "email": "email@email.com"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_EMAIL_VALIDATION_BIGDATACORP",
  "email": "email@email.com"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_EMAIL_VALIDATION_BIGDATACORP",
  "email": "email@email.com"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `email` | Sim | Parâmetro usado pelo service SERVICE_EMAIL_VALIDATION_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_EMAIL_VALIDATION_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Dados cadastrais e Receita Federal

<AccordionGroup>
<Accordion title="Consulta de MEI">

**Service:** `SERVICE_MEI_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Consulta de MEI" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_MEI_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEI_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEI_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_MEI_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_MEI_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="CPF na Receita Federal on-demand">

**Service:** `SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP`

**Quando usar:** Use para consultar ou validar dados cadastrais da pessoa em bases da Receita Federal.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Dados PIS">

**Service:** `SERVICE_PIS_CONSULTATION_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Dados PIS" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PIS_CONSULTATION_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PIS_CONSULTATION_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PIS_CONSULTATION_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PIS_CONSULTATION_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PIS_CONSULTATION_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Enriquecimento de dados">

**Service:** `SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP`

**Quando usar:** Use para complementar dados cadastrais da pessoa a partir do documento informado.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Status do CPF na Receita Federal">

**Service:** `SERVICE_RFB_PF_BIGDATACORP`

**Quando usar:** Use para consultar ou validar dados cadastrais da pessoa em bases da Receita Federal.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** `dataDeNascimento`

### Body

```json
{
  "service": "SERVICE_RFB_PF_BIGDATACORP",
  "cpf": "cpf",
  "dataDeNascimento": "yyyy-MM-dd (opcional)"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PF_BIGDATACORP",
  "cpf": "cpf",
  "dataDeNascimento": "yyyy-MM-dd (opcional)"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PF_BIGDATACORP",
  "cpf": "cpf",
  "dataDeNascimento": "yyyy-MM-dd (opcional)"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_RFB_PF_BIGDATACORP. |
| `dataDeNascimento` | Não | Parâmetro usado pelo service SERVICE_RFB_PF_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_RFB_PF_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Validação do E-Social">

**Service:** `SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Validação do E-Social" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** `nit`

### Body

```json
{
  "service": "SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP",
  "cpf": "cpf",
  "nit": "(opcional)"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP",
  "cpf": "cpf",
  "nit": "(opcional)"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP",
  "cpf": "cpf",
  "nit": "(opcional)"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP. |
| `nit` | Não | Parâmetro usado pelo service SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Dados eleitorais e PEP

<AccordionGroup>
<Accordion title="Dados eleitorais de candidato">

**Service:** `SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP`

**Quando usar:** Use para consultar informações eleitorais relacionadas à pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Doações eleitorais PF (BigDataCorp)">

**Service:** `SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP`

**Quando usar:** Use para consultar informações eleitorais relacionadas à pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Envolvimento político (BigDataCorp)">

**Service:** `SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Envolvimento político (BigDataCorp)" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Envolvimento político PF (BigDataCorp)">

**Service:** `SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Envolvimento político PF (BigDataCorp)" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Histórico político familiar PF (BigDataCorp)">

**Service:** `SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Histórico político familiar PF (BigDataCorp)" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Pessoa politicamente exposta">

**Service:** `SERVICE_PEP`

**Quando usar:** Use para verificar exposição política ou vínculo com Pessoa Politicamente Exposta.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PEP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PEP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PEP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PEP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PEP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Prestadores de serviços eleitorais PF (BigDataCorp)">

**Service:** `SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP`

**Quando usar:** Use para consultar informações eleitorais relacionadas à pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Jurídico, certidões e protestos

<AccordionGroup>
<Accordion title="Antecedentes criminais civis">

**Service:** `SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP`

**Quando usar:** Use para consultar certidões, processos ou informações jurídicas da pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`, `rg`, `uf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP",
  "cpf": "cpf",
  "rg": "rg",
  "uf": "uf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP",
  "cpf": "cpf",
  "rg": "rg",
  "uf": "uf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP",
  "cpf": "cpf",
  "rg": "rg",
  "uf": "uf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP. |
| `rg` | Sim | Parâmetro usado pelo service SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP. |
| `uf` | Sim | Parâmetro usado pelo service SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Antecedentes criminais federais">

**Service:** `SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP`

**Quando usar:** Use para consultar certidões, processos ou informações jurídicas da pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Certidão de Nada Consta">

**Service:** `SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP`

**Quando usar:** Use para consultar certidões, processos ou informações jurídicas da pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`, `court`, `uf`, `sphere`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP",
  "cpf": "cpf",
  "court": "TRF1",
  "uf": "uf",
  "sphere": "CIVIL"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP",
  "cpf": "cpf",
  "court": "TRF1",
  "uf": "uf",
  "sphere": "CIVIL"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP",
  "cpf": "cpf",
  "court": "TRF1",
  "uf": "uf",
  "sphere": "CIVIL"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP. |
| `court` | Sim | Parâmetro usado pelo service SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP. |
| `uf` | Sim | Parâmetro usado pelo service SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP. |
| `sphere` | Sim | Parâmetro usado pelo service SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Certidão negativa de protesto">

**Service:** `SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP`

**Quando usar:** Use para consultar protestos associados ao documento da pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Certidão negativa de protesto PF (InfoSimples)">

**Service:** `SERVICE_PROTEST_PF_INFOSIMPLES`

**Quando usar:** Use para consultar protestos associados ao documento da pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PROTEST_PF_INFOSIMPLES",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PF_INFOSIMPLES",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PF_INFOSIMPLES",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PROTEST_PF_INFOSIMPLES. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PROTEST_PF_INFOSIMPLES."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Certidão negativa de protesto PF (Netrin)">

**Service:** `SERVICE_PROTEST_PF_NETRIN`

**Quando usar:** Use para consultar protestos associados ao documento da pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PROTEST_PF_NETRIN",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PF_NETRIN",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PF_NETRIN",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PROTEST_PF_NETRIN. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PROTEST_PF_NETRIN."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Mandado de prisão">

**Service:** `SERVICE_ARREST_WARRANT`

**Quando usar:** Use este service quando precisar executar a consulta "Mandado de prisão" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `nome`, `motherName`, `fatherName`, `birthDate`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ARREST_WARRANT",
  "nome": "nome",
  "motherName": "nome da mãe",
  "fatherName": "nome do pai",
  "birthDate": "dd/MM/yyyy",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ARREST_WARRANT",
  "nome": "nome",
  "motherName": "nome da mãe",
  "fatherName": "nome do pai",
  "birthDate": "dd/MM/yyyy",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ARREST_WARRANT",
  "nome": "nome",
  "motherName": "nome da mãe",
  "fatherName": "nome do pai",
  "birthDate": "dd/MM/yyyy",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `nome` | Sim | Parâmetro usado pelo service SERVICE_ARREST_WARRANT. |
| `motherName` | Sim | Parâmetro usado pelo service SERVICE_ARREST_WARRANT. |
| `fatherName` | Sim | Parâmetro usado pelo service SERVICE_ARREST_WARRANT. |
| `birthDate` | Sim | Parâmetro usado pelo service SERVICE_ARREST_WARRANT. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_ARREST_WARRANT. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ARREST_WARRANT."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Processos jurídicos e administrativos">

**Service:** `SERVICE_JURIDICAL_PROCESSES_BIGDATACORP`

**Quando usar:** Use para consultar certidões, processos ou informações jurídicas da pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_JURIDICAL_PROCESSES_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_JURIDICAL_PROCESSES_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_JURIDICAL_PROCESSES_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_JURIDICAL_PROCESSES_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_JURIDICAL_PROCESSES_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### KYC, compliance e exposição

<AccordionGroup>
<Accordion title="Exposição e perfil na mídia PF (BigDataCorp)">

**Service:** `SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Exposição e perfil na mídia PF (BigDataCorp)" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="KYC e compliance">

**Service:** `SERVICE_PERSON_KYC_BIGDATACORP`

**Quando usar:** Use para executar checagens de KYC e compliance da pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** `birthDate`

### Body

```json
{
  "service": "SERVICE_PERSON_KYC_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_KYC_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_KYC_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PERSON_KYC_BIGDATACORP. |
| `birthDate` | Não | Parâmetro usado pelo service SERVICE_PERSON_KYC_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PERSON_KYC_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Propensão a apostas online">

**Service:** `SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Propensão a apostas online" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Outros services

<AccordionGroup>
<Accordion title="Histórico profissional">

**Service:** `SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Histórico profissional" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Histórico profissional do titular">

**Service:** `SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Histórico profissional do titular" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** `birthDate`

### Body

```json
{
  "service": "SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP. |
| `birthDate` | Não | Parâmetro usado pelo service SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Modelagem de dados">

**Service:** `SERVICE_PERSON_DATA_MODELING_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Modelagem de dados" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PERSON_DATA_MODELING_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_DATA_MODELING_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_DATA_MODELING_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PERSON_DATA_MODELING_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PERSON_DATA_MODELING_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Prompt de IA para pessoa">

**Service:** `SERVICE_PERSON_AI_PROMPT_OPENAI`

**Quando usar:** Use este service quando precisar executar a consulta "Prompt de IA para pessoa" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PERSON_AI_PROMPT_OPENAI",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_AI_PROMPT_OPENAI",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_AI_PROMPT_OPENAI",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PERSON_AI_PROMPT_OPENAI. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PERSON_AI_PROMPT_OPENAI."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Servidores públicos">

**Service:** `SERVICE_PUBLIC_SERVANTS_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Servidores públicos" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PUBLIC_SERVANTS_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PUBLIC_SERVANTS_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PUBLIC_SERVANTS_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_PUBLIC_SERVANTS_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PUBLIC_SERVANTS_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Risco, crédito e dívidas

<AccordionGroup>
<Accordion title="Dívida ativa">

**Service:** `SERVICE_ACTIVE_DEBT_PF_BIGDATACORP`

**Quando usar:** Use para consultar débitos ou dívidas associadas à pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ACTIVE_DEBT_PF_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ACTIVE_DEBT_PF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ACTIVE_DEBT_PF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_ACTIVE_DEBT_PF_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ACTIVE_DEBT_PF_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Informações financeiras">

**Service:** `SERVICE_FINANCIAL_INFORMATION_BIGDATACORP`

**Quando usar:** Use para consultar informações financeiras associadas à pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_FINANCIAL_INFORMATION_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FINANCIAL_INFORMATION_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FINANCIAL_INFORMATION_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_FINANCIAL_INFORMATION_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_FINANCIAL_INFORMATION_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Risco financeiro">

**Service:** `SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP`

**Quando usar:** Use para consultar informações financeiras associadas à pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** `birthDate`

### Body

```json
{
  "service": "SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP. |
| `birthDate` | Não | Parâmetro usado pelo service SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Score de inadimplência">

**Service:** `SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP`

**Quando usar:** Use para avaliar risco, score ou propensão associada à pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Score de risco de fraude">

**Service:** `SERVICE_FRAUD_RISK_SCORE_BIGDATACORP`

**Quando usar:** Use para avaliar risco, score ou propensão associada à pessoa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cpf`, `factor`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_FRAUD_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf",
  "factor": "minRisk or minattrition"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FRAUD_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf",
  "factor": "minRisk or minattrition"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FRAUD_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf",
  "factor": "minRisk or minattrition"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cpf` | Sim | Parâmetro usado pelo service SERVICE_FRAUD_RISK_SCORE_BIGDATACORP. |
| `factor` | Sim | Parâmetro usado pelo service SERVICE_FRAUD_RISK_SCORE_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_FRAUD_RISK_SCORE_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>


## Padrões de erro

Os exemplos abaixo mostram formatos comuns. A mensagem pode variar conforme validação, produto e ambiente.

<AccordionGroup>
<Accordion title="Token ausente ou inválido">

```json
{
  "status": {
    "code": 401,
    "message": "Unauthorized"
  }
}
```

</Accordion>
<Accordion title="Parâmetro obrigatório ausente">

```json
{
  "status": {
    "code": 400,
    "message": "Required field is missing or invalid"
  }
}
```

</Accordion>
<Accordion title="Service não liberado ou indisponível">

```json
{
  "status": {
    "code": 403,
    "message": "Service unavailable or not enabled for this client"
  }
}
```

</Accordion>
</AccordionGroup>