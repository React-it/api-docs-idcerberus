---
title: Services de pessoa jurídica
description: Catálogo explícito dos services de pessoa jurídica disponíveis via API, com campos esperados e exemplos de request.
---

# Services de pessoa jurídica

Catálogo explícito dos services de pessoa jurídica disponíveis via API, com campos esperados e exemplos de request.

<Info>
Todos os services abaixo usam o mesmo endpoint: `POST /api/service-api`. O produto executado é definido pelo campo `service` no body da requisição.
</Info>

## Como ler esta referência

- **Nome**: nome funcional do produto.
- **Service**: valor exato que deve ser enviado no campo `service`.
- **Família**: agrupamento por objetivo de uso, como dados cadastrais, risco, jurídico ou biometria.
- **Campos**: parâmetros esperados no body além de `service`.
- **Retorno principal**: resumo dos dados esperados no objeto `result` para aquele service.
- **Exemplos**: cada item traz body JSON, curl de homologação, curl de produção e response resumido.

## Índice por família

### Contatos, sites e relacionamentos

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Doações eleitorais dos sócios (BigDataCorp) | `SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP` | `cnpj` | Use para consultar informações eleitorais relacionadas à empresa. | Retorna vinculos, socios, relacionamentos economicos ou pessoas/empresas relacionadas ao pessoa/CPF. |
| Endereços estendidos CNPJ (BigDataCorp) | `SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP` | `cnpj` | Use para consultar ou validar endereços associados ao documento. | Retorna enderecos encontrados ou resultado de validacao de endereco associado ao pessoa/CPF. |
| KYC e compliance dos sócios | `SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP` | `cnpj` | Use para executar checagens de KYC e compliance da empresa. | Retorna indicadores de KYC/compliance do pessoa/CPF, como PEP, sancoes, exposicao, historicos e sinais de risco quando disponiveis. |
| Processos jurídicos dos sócios | `SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP` | `cnpj` | Use para consultar certidões, processos ou informações jurídicas da empresa. | Retorna ocorrencias juridicas, certidoes, protestos, antecedentes ou mandados associados ao pessoa/CPF, alem do status da consulta. |
| Relacionamentos da empresa | `SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP` | `cnpj` | Use para consultar vínculos, sócios ou relacionamentos associados à empresa. | Retorna vinculos, socios, relacionamentos economicos ou pessoas/empresas relacionadas ao pessoa/CPF. |
| Sócios de primeiro nível | `SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP` | `cnpj` | Use para consultar vínculos, sócios ou relacionamentos associados à empresa. | Retorna vinculos, socios, relacionamentos economicos ou pessoas/empresas relacionadas ao pessoa/CPF. |
| Sócios na Receita Federal | `SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP` | `cnpj` | Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal. | Retorna dados cadastrais do pessoa/CPF, incluindo status cadastral, identificacao, datas e atributos disponiveis na base consultada. |

### Dados cadastrais e Receita Federal

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| CNPJ na Receita Federal on-demand | `SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP` | `cnpj` | Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal. | Retorna dados cadastrais do pessoa/CPF, incluindo status cadastral, identificacao, datas e atributos disponiveis na base consultada. |
| DAS MEI na Receita | `SERVICE_DAS_MEI_INFOSIMPLES` | `cnpj` | Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal. | Retorna dados cadastrais do pessoa/CPF, incluindo status cadastral, identificacao, datas e atributos disponiveis na base consultada. |
| Enriquecimento de dados | `SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP` | `cnpj` | Use para complementar dados cadastrais da empresa a partir do documento informado. | Retorna dados cadastrais do pessoa/CPF, incluindo status cadastral, identificacao, datas e atributos disponiveis na base consultada. |
| SINTEGRA | `SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP` | `cnpj`, `uf` | Use este service quando precisar executar a consulta "SINTEGRA" via API. | Retorna dados cadastrais ou fiscais especificos do pessoa/CPF, conforme a base consultada pelo service. |
| Status do CNPJ na Receita Federal | `SERVICE_RFB_PJ_BIGDATACORP` | `cnpj` | Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal. | Retorna dados cadastrais do pessoa/CPF, incluindo status cadastral, identificacao, datas e atributos disponiveis na base consultada. |

### Dados eleitorais e PEP

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Doações eleitorais PJ (BigDataCorp) | `SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP` | `cnpj` | Use para consultar informações eleitorais relacionadas à empresa. | Retorna dados eleitorais associados ao pessoa/CPF, como candidaturas, doacoes, fornecedores ou historico politico quando disponivel. |
| Fornecedores eleitorais PJ (BigDataCorp) | `SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP` | `cnpj` | Use para consultar informações eleitorais relacionadas à empresa. | Retorna dados eleitorais associados ao pessoa/CPF, como candidaturas, doacoes, fornecedores ou historico politico quando disponivel. |

### Jurídico, certidões e protestos

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Certidão negativa de protesto | `SERVICE_PROTEST_PJ_INFOSIMPLES` | `cnpj` | Use para consultar protestos associados ao documento da empresa. | Retorna ocorrencias juridicas, certidoes, protestos, antecedentes ou mandados associados ao pessoa/CPF, alem do status da consulta. |
| Certidão negativa de protesto PJ (Netrin) | `SERVICE_PROTEST_PJ_NETRIN` | `cnpj` | Use para consultar protestos associados ao documento da empresa. | Retorna ocorrencias juridicas, certidoes, protestos, antecedentes ou mandados associados ao pessoa/CPF, alem do status da consulta. |

### KYC, compliance e exposição

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Compliance de casas de apostas PJ (BigDataCorp) | `SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP` | `cnpj` | Use para executar checagens de KYC e compliance da empresa. | Retorna indicadores de KYC/compliance do pessoa/CPF, como PEP, sancoes, exposicao, historicos e sinais de risco quando disponiveis. |
| Exposição e perfil na mídia PJ (BigDataCorp) | `SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP` | `cnpj` | Use este service quando precisar executar a consulta "Exposição e perfil na mídia PJ (BigDataCorp)" via API. | Retorna o objeto result do service SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP com os dados disponiveis para a consulta, alem do status de processamento. |

### Risco, crédito e dívidas

| Nome | Service | Campos | Quando usar | Retorno principal |
| --- | --- | --- | --- | --- |
| Débitos ativos | `SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP` | `cnpj` | Use para consultar débitos ou dívidas associadas à empresa. | Retorna indicadores financeiros e de risco do pessoa/CPF, como scores, debitos, renda/ativos estimados e sinais de inadimplencia quando aplicavel. |


## Requests completos

### Contatos, sites e relacionamentos

<AccordionGroup>
<Accordion title="Doações eleitorais dos sócios (BigDataCorp)">

**Service:** `SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP`

**Quando usar:** Use para consultar informações eleitorais relacionadas à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Endereços estendidos CNPJ (BigDataCorp)">

**Service:** `SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP`

**Quando usar:** Use para consultar ou validar endereços associados ao documento.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="KYC e compliance dos sócios">

**Service:** `SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP`

**Quando usar:** Use para executar checagens de KYC e compliance da empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Processos jurídicos dos sócios">

**Service:** `SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP`

**Quando usar:** Use para consultar certidões, processos ou informações jurídicas da empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Relacionamentos da empresa">

**Service:** `SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP`

**Quando usar:** Use para consultar vínculos, sócios ou relacionamentos associados à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Sócios de primeiro nível">

**Service:** `SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP`

**Quando usar:** Use para consultar vínculos, sócios ou relacionamentos associados à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Sócios na Receita Federal">

**Service:** `SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP`

**Quando usar:** Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Dados cadastrais e Receita Federal

<AccordionGroup>
<Accordion title="CNPJ na Receita Federal on-demand">

**Service:** `SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP`

**Quando usar:** Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="DAS MEI na Receita">

**Service:** `SERVICE_DAS_MEI_INFOSIMPLES`

**Quando usar:** Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_DAS_MEI_INFOSIMPLES",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DAS_MEI_INFOSIMPLES",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DAS_MEI_INFOSIMPLES",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_DAS_MEI_INFOSIMPLES. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_DAS_MEI_INFOSIMPLES."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Enriquecimento de dados">

**Service:** `SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP`

**Quando usar:** Use para complementar dados cadastrais da empresa a partir do documento informado.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="SINTEGRA">

**Service:** `SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "SINTEGRA" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** `uf`

### Body

```json
{
  "service": "SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP",
  "cnpj": "cnpj",
  "uf": "uf (opcional)"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP",
  "cnpj": "cnpj",
  "uf": "uf (opcional)"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP",
  "cnpj": "cnpj",
  "uf": "uf (opcional)"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP. |
| `uf` | Não | Parâmetro usado pelo service SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Status do CNPJ na Receita Federal">

**Service:** `SERVICE_RFB_PJ_BIGDATACORP`

**Quando usar:** Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_RFB_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_RFB_PJ_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_RFB_PJ_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Dados eleitorais e PEP

<AccordionGroup>
<Accordion title="Doações eleitorais PJ (BigDataCorp)">

**Service:** `SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP`

**Quando usar:** Use para consultar informações eleitorais relacionadas à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Fornecedores eleitorais PJ (BigDataCorp)">

**Service:** `SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP`

**Quando usar:** Use para consultar informações eleitorais relacionadas à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Jurídico, certidões e protestos

<AccordionGroup>
<Accordion title="Certidão negativa de protesto">

**Service:** `SERVICE_PROTEST_PJ_INFOSIMPLES`

**Quando usar:** Use para consultar protestos associados ao documento da empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PROTEST_PJ_INFOSIMPLES",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PJ_INFOSIMPLES",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PJ_INFOSIMPLES",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_PROTEST_PJ_INFOSIMPLES. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PROTEST_PJ_INFOSIMPLES."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Certidão negativa de protesto PJ (Netrin)">

**Service:** `SERVICE_PROTEST_PJ_NETRIN`

**Quando usar:** Use para consultar protestos associados ao documento da empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_PROTEST_PJ_NETRIN",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PJ_NETRIN",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PJ_NETRIN",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_PROTEST_PJ_NETRIN. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_PROTEST_PJ_NETRIN."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### KYC, compliance e exposição

<AccordionGroup>
<Accordion title="Compliance de casas de apostas PJ (BigDataCorp)">

**Service:** `SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP`

**Quando usar:** Use para executar checagens de KYC e compliance da empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Exposição e perfil na mídia PJ (BigDataCorp)">

**Service:** `SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP`

**Quando usar:** Use este service quando precisar executar a consulta "Exposição e perfil na mídia PJ (BigDataCorp)" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Risco, crédito e dívidas

<AccordionGroup>
<Accordion title="Débitos ativos">

**Service:** `SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP`

**Quando usar:** Use para consultar débitos ou dívidas associadas à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>


## Padrões de erro

Os exemplos abaixo mostram formatos comuns. A mensagem pode variar conforme validação, produto e ambiente.

<AccordionGroup>
<Accordion title="Token ausente ou inválido">

```json
{
  "status": {
    "code": 401,
    "message": "Unauthorized"
  }
}
```

</Accordion>
<Accordion title="Parâmetro obrigatório ausente">

```json
{
  "status": {
    "code": 400,
    "message": "Required field is missing or invalid"
  }
}
```

</Accordion>
<Accordion title="Service não liberado ou indisponível">

```json
{
  "status": {
    "code": 403,
    "message": "Service unavailable or not enabled for this client"
  }
}
```

</Accordion>
</AccordionGroup>