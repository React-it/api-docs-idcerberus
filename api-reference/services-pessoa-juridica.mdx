---
title: Services de pessoa jurídica
description: Catálogo explícito dos services de pessoa jurídica disponíveis via API, com campos esperados e exemplos de request.
---

# Services de pessoa jurídica

Catálogo explícito dos services de pessoa jurídica disponíveis via API, com campos esperados e exemplos de request.

<Info>
Todos os services abaixo usam o mesmo endpoint: `POST /api/service-api`. O produto executado é definido pelo campo `service` no body da requisição.
</Info>

## Como ler esta referência

- **Nome**: nome funcional do produto.
- **Service**: valor exato que deve ser enviado no campo `service`.
- **Família**: agrupamento por objetivo de uso, como dados cadastrais, risco, jurídico ou biometria.
- **Campos**: parâmetros esperados no body além de `service`.
- **Exemplos**: cada item traz body JSON, curl de homologação, curl de produção e response resumido.

## Índice por família

### Contatos, sites e relacionamentos

| Nome | Service | Campos | Quando usar |
| --- | --- | --- | --- |
| Relacionamentos da empresa | `service_company_relationship` | `cnpj` | Use para consultar vínculos, sócios ou relacionamentos associados à empresa. |

### Dados cadastrais e Receita Federal

| Nome | Service | Campos | Quando usar |
| --- | --- | --- | --- |
| CNPJ na Receita Federal on-demand | `SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP` | `cnpj` | Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal. |
| Dados de sites | `SERVICE_DOMAINS_CNPJ` | `cnpj` | Use para consultar dados de sites vinculados à empresa. |
| DAS MEI na Receita | `service_das_mei` | `cnpj` | Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal. |
| Endereços estendidos | `SERVICE_ADDRESSES_EXTENDED_CNPJ` | `cnpj` | Use para consultar ou validar endereços associados ao documento. |
| Enriquecimento de dados | `service_corporate_data_enrichment` | `cnpj` | Use para complementar dados cadastrais da empresa a partir do documento informado. |
| SINTEGRA | `service_sintegra_consultation` | `cnpj`, `uf` | Use este service quando precisar executar a consulta "SINTEGRA" via API. |
| Sócios de primeiro nível | `service_first_level_partner` | `cnpj` | Use para consultar vínculos, sócios ou relacionamentos associados à empresa. |
| Sócios na Receita Federal | `SERVICE_COMPANY_RFB_OWNERS` | `cnpj` | Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal. |
| Status do CNPJ na Receita Federal | `service_rfb_pj` | `cnpj` | Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal. |

### Dados eleitorais e PEP

| Nome | Service | Campos | Quando usar |
| --- | --- | --- | --- |
| Doações eleitorais | `SERVICE_ELECTORAL_DONORS_CNPJ` | `cnpj` | Use para consultar informações eleitorais relacionadas à empresa. |
| Doações eleitorais dos sócios | `SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ` | `cnpj` | Use para consultar informações eleitorais relacionadas à empresa. |
| Fornecedores eleitorais | `SERVICE_ELECTORAL_PROVIDERS_CNPJ` | `cnpj` | Use para consultar informações eleitorais relacionadas à empresa. |

### Jurídico, certidões e protestos

| Nome | Service | Campos | Quando usar |
| --- | --- | --- | --- |
| Certidão negativa de protesto | `service_protest_clearance_certificate_pj` | `cnpj` | Use para consultar protestos associados ao documento da empresa. |
| Processos jurídicos dos sócios | `service_juridical_processes_pj_owners` | `cnpj` | Use para consultar certidões, processos ou informações jurídicas da empresa. |

### KYC, compliance e exposição

| Nome | Service | Campos | Quando usar |
| --- | --- | --- | --- |
| Compliance de casas de apostas | `SERVICE_COMPLIANCE_BET_PJ` | `cnpj` | Use para executar checagens de KYC e compliance da empresa. |
| Exposição e perfil na mídia dos sócios | `SERVICE_MEDIA_PROFILE_EXPOSURE_PJ` | `cnpj` | Use para consultar vínculos, sócios ou relacionamentos associados à empresa. |
| KYC e compliance dos sócios | `service_company_kyc_owners` | `cnpj` | Use para executar checagens de KYC e compliance da empresa. |

### Risco, crédito e dívidas

| Nome | Service | Campos | Quando usar |
| --- | --- | --- | --- |
| Débitos ativos | `SERVICE_ACTIVE_DEBT_PJ` | `cnpj` | Use para consultar débitos ou dívidas associadas à empresa. |


## Requests completos

### Contatos, sites e relacionamentos

<AccordionGroup>
<Accordion title="Relacionamentos da empresa">

**Service:** `service_company_relationship`

**Quando usar:** Use para consultar vínculos, sócios ou relacionamentos associados à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "service_company_relationship",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_company_relationship",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_company_relationship",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service service_company_relationship. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service service_company_relationship."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Dados cadastrais e Receita Federal

<AccordionGroup>
<Accordion title="CNPJ na Receita Federal on-demand">

**Service:** `SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP`

**Quando usar:** Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Dados de sites">

**Service:** `SERVICE_DOMAINS_CNPJ`

**Quando usar:** Use para consultar dados de sites vinculados à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_DOMAINS_CNPJ",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DOMAINS_CNPJ",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DOMAINS_CNPJ",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_DOMAINS_CNPJ. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_DOMAINS_CNPJ."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="DAS MEI na Receita">

**Service:** `service_das_mei`

**Quando usar:** Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "service_das_mei",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_das_mei",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_das_mei",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service service_das_mei. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service service_das_mei."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Endereços estendidos">

**Service:** `SERVICE_ADDRESSES_EXTENDED_CNPJ`

**Quando usar:** Use para consultar ou validar endereços associados ao documento.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ADDRESSES_EXTENDED_CNPJ",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ADDRESSES_EXTENDED_CNPJ",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ADDRESSES_EXTENDED_CNPJ",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_ADDRESSES_EXTENDED_CNPJ. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ADDRESSES_EXTENDED_CNPJ."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Enriquecimento de dados">

**Service:** `service_corporate_data_enrichment`

**Quando usar:** Use para complementar dados cadastrais da empresa a partir do documento informado.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "service_corporate_data_enrichment",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_corporate_data_enrichment",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_corporate_data_enrichment",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service service_corporate_data_enrichment. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service service_corporate_data_enrichment."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="SINTEGRA">

**Service:** `service_sintegra_consultation`

**Quando usar:** Use este service quando precisar executar a consulta "SINTEGRA" via API.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** `uf`

### Body

```json
{
  "service": "service_sintegra_consultation",
  "cnpj": "cnpj",
  "uf": "uf (opcional)"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_sintegra_consultation",
  "cnpj": "cnpj",
  "uf": "uf (opcional)"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_sintegra_consultation",
  "cnpj": "cnpj",
  "uf": "uf (opcional)"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service service_sintegra_consultation. |
| `uf` | Não | Parâmetro usado pelo service service_sintegra_consultation. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service service_sintegra_consultation."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Sócios de primeiro nível">

**Service:** `service_first_level_partner`

**Quando usar:** Use para consultar vínculos, sócios ou relacionamentos associados à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "service_first_level_partner",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_first_level_partner",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_first_level_partner",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service service_first_level_partner. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service service_first_level_partner."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Sócios na Receita Federal">

**Service:** `SERVICE_COMPANY_RFB_OWNERS`

**Quando usar:** Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_COMPANY_RFB_OWNERS",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_RFB_OWNERS",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_RFB_OWNERS",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_COMPANY_RFB_OWNERS. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_COMPANY_RFB_OWNERS."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Status do CNPJ na Receita Federal">

**Service:** `service_rfb_pj`

**Quando usar:** Use para consultar ou validar dados cadastrais da empresa em bases da Receita Federal.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "service_rfb_pj",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_rfb_pj",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_rfb_pj",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service service_rfb_pj. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service service_rfb_pj."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Dados eleitorais e PEP

<AccordionGroup>
<Accordion title="Doações eleitorais">

**Service:** `SERVICE_ELECTORAL_DONORS_CNPJ`

**Quando usar:** Use para consultar informações eleitorais relacionadas à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ELECTORAL_DONORS_CNPJ",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_DONORS_CNPJ",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_DONORS_CNPJ",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_ELECTORAL_DONORS_CNPJ. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ELECTORAL_DONORS_CNPJ."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Doações eleitorais dos sócios">

**Service:** `SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ`

**Quando usar:** Use para consultar informações eleitorais relacionadas à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Fornecedores eleitorais">

**Service:** `SERVICE_ELECTORAL_PROVIDERS_CNPJ`

**Quando usar:** Use para consultar informações eleitorais relacionadas à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CNPJ",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CNPJ",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CNPJ",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_ELECTORAL_PROVIDERS_CNPJ. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ELECTORAL_PROVIDERS_CNPJ."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Jurídico, certidões e protestos

<AccordionGroup>
<Accordion title="Certidão negativa de protesto">

**Service:** `service_protest_clearance_certificate_pj`

**Quando usar:** Use para consultar protestos associados ao documento da empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "service_protest_clearance_certificate_pj",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_protest_clearance_certificate_pj",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_protest_clearance_certificate_pj",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service service_protest_clearance_certificate_pj. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service service_protest_clearance_certificate_pj."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Processos jurídicos dos sócios">

**Service:** `service_juridical_processes_pj_owners`

**Quando usar:** Use para consultar certidões, processos ou informações jurídicas da empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "service_juridical_processes_pj_owners",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_juridical_processes_pj_owners",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_juridical_processes_pj_owners",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service service_juridical_processes_pj_owners. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service service_juridical_processes_pj_owners."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### KYC, compliance e exposição

<AccordionGroup>
<Accordion title="Compliance de casas de apostas">

**Service:** `SERVICE_COMPLIANCE_BET_PJ`

**Quando usar:** Use para executar checagens de KYC e compliance da empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_COMPLIANCE_BET_PJ",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPLIANCE_BET_PJ",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPLIANCE_BET_PJ",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_COMPLIANCE_BET_PJ. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_COMPLIANCE_BET_PJ."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="Exposição e perfil na mídia dos sócios">

**Service:** `SERVICE_MEDIA_PROFILE_EXPOSURE_PJ`

**Quando usar:** Use para consultar vínculos, sócios ou relacionamentos associados à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PJ",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PJ",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PJ",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_MEDIA_PROFILE_EXPOSURE_PJ. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_MEDIA_PROFILE_EXPOSURE_PJ."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

<Accordion title="KYC e compliance dos sócios">

**Service:** `service_company_kyc_owners`

**Quando usar:** Use para executar checagens de KYC e compliance da empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "service_company_kyc_owners",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_company_kyc_owners",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "service_company_kyc_owners",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service service_company_kyc_owners. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service service_company_kyc_owners."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>

### Risco, crédito e dívidas

<AccordionGroup>
<Accordion title="Débitos ativos">

**Service:** `SERVICE_ACTIVE_DEBT_PJ`

**Quando usar:** Use para consultar débitos ou dívidas associadas à empresa.

**Endpoint:** `POST /api/service-api`

**Campos obrigatórios:** `service`, `cnpj`

**Campos opcionais:** Nenhum campo opcional mapeado neste exemplo.

### Body

```json
{
  "service": "SERVICE_ACTIVE_DEBT_PJ",
  "cnpj": "cnpj"
}
```

### Homologação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ACTIVE_DEBT_PJ",
  "cnpj": "cnpj"
}'
```

### Produção

```bash
curl --location 'https://backoffice.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ACTIVE_DEBT_PJ",
  "cnpj": "cnpj"
}'
```

### Campos do body

| Campo | Obrigatório | Descrição |
| --- | --- | --- |
| `service` | Sim | Código do produto que será executado. |
| `cnpj` | Sim | Parâmetro usado pelo service SERVICE_ACTIVE_DEBT_PJ. |

### Response resumido

```json
{
  "result": {
    "observacao": "Os campos retornados variam conforme o service SERVICE_ACTIVE_DEBT_PJ."
  },
  "status": {
    "code": 200,
    "message": "Success"
  },
  "externalId": "{externalId}"
}
```

O objeto `result` muda de acordo com o produto. Para exemplos completos de retorno, consulte também o endpoint geral `POST /api/service-api` no OpenAPI.

</Accordion>

</AccordionGroup>


## Padrões de erro

Os exemplos abaixo mostram formatos comuns. A mensagem pode variar conforme validação, produto e ambiente.

<AccordionGroup>
<Accordion title="Token ausente ou inválido">

```json
{
  "status": {
    "code": 401,
    "message": "Unauthorized"
  }
}
```

</Accordion>
<Accordion title="Parâmetro obrigatório ausente">

```json
{
  "status": {
    "code": 400,
    "message": "Required field is missing or invalid"
  }
}
```

</Accordion>
<Accordion title="Service não liberado ou indisponível">

```json
{
  "status": {
    "code": 403,
    "message": "Service unavailable or not enabled for this client"
  }
}
```

</Accordion>
</AccordionGroup>