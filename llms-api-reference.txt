# idCerberus API Reference - resumo operacional para LLM

Use este arquivo para gerar exemplos de request, explicar chamadas da API e escolher o `service` correto sem depender do OpenAPI completo.

## Regras obrigatórias

- Não invente endpoints, parâmetros ou services.
- Para consultas externas, use `POST /api/service-api` e envie o campo `service` no body.
- Use homologação para testes: `https://backoffice-hml.idcerberus.com`.
- Use produção somente quando o usuário pedir explicitamente: `https://backoffice.idcerberus.com`.
- Nunca exponha `client`, `secret`, JWT real, CPF real, CNPJ real ou imagens reais em exemplos.
- Quando faltar um service no catálogo, diga que ele precisa ser confirmado antes de documentar ou integrar.

## Autenticação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/token-generate' \
--header 'Content-Type: application/json' \
--data '{
  "client": "{client}",
  "secret": "{secret}"
}'
```

## Pessoa Física

### Antecedentes criminais civis

- Service: `SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `rg`, `uf`
- Retorno principal: Retorna resultado de antecedentes criminais civis, com status da certidão, ocorrências encontradas, UF, RG e mensagens da consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP",
  "cpf": "cpf",
  "rg": "rg",
  "uf": "uf"
}'
```

### Antecedentes criminais federais

- Service: `SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna resultado de antecedentes criminais federais, com status da certidão, ocorrências encontradas e mensagens da consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Benefícios sociais estendidos

- Service: `SERVICE_SOCIAL_ASSISTANCE_EXTENDED`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna beneficios sociais estendidos vinculados ao CPF, com programas, indicadores, situação e detalhes encontrados quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_SOCIAL_ASSISTANCE_EXTENDED",
  "cpf": "cpf"
}'
```

### Benefícios sociais familiares

- Service: `SERVICE_FAMILY_SOCIAL_BENEFITS`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna beneficios sociais familiares vinculados ao CPF, com programas, situação, quantidade e registros encontrados quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FAMILY_SOCIAL_BENEFITS",
  "cpf": "cpf"
}'
```

### Busca de face na base

- Service: `SERVICE_FACE_INDEX`
- Endpoint: `POST /api/service-api`
- Campos do request: `image1`
- Retorno principal: Busca uma selfie na base de faces indexadas e retorna se encontrou face, CPF associado e similaridade quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FACE_INDEX",
  "image1": "base64"
}'
```

### Certidão de Nada Consta

- Service: `SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `court`, `uf`, `sphere`
- Retorno principal: Retorna certidão de nada consta para a esfera/tribunal informado, com status, mensagem, ocorrências e dados usados na consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP",
  "cpf": "cpf",
  "court": "TRF1",
  "uf": "uf",
  "sphere": "CIVIL"
}'
```

### Certidão negativa de protesto

- Service: `SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna certidão/consulta de protestos para CPF, com status de nada consta ou lista de protestos, cartório, valor e datas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Certidão negativa de protesto PF (InfoSimples)

- Service: `SERVICE_PROTEST_PF_INFOSIMPLES`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna certidão/consulta de protestos para CPF via InfoSimples, com status, cartórios consultados, protestos e mensagens.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PF_INFOSIMPLES",
  "cpf": "cpf"
}'
```

### Certidão negativa de protesto PF (Netrin)

- Service: `SERVICE_PROTEST_PF_NETRIN`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna certidão/consulta de protestos para CPF via Netrin, com status, cartórios consultados, protestos e mensagens.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PF_NETRIN",
  "cpf": "cpf"
}'
```

### Consulta de MEI

- Service: `SERVICE_MEI_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna empresas MEI associadas ao CPF, incluindo CNPJ, razão social, situação, atividades, endereço e datas cadastrais quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEI_BIGDATACORP",
  "cpf": "cpf"
}'
```

### CPF na Receita Federal on-demand

- Service: `SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna situação atualizada do CPF consultada sob demanda na Receita Federal, com nome, nascimento, status cadastral e protocolo.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Dados demográficos

- Service: `SERVICE_DEMOGRAPHIC_DATA_CPF`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`
- Retorno principal: Retorna dados demograficos associados ao CPF, com dados regionais, estimativas e indicadores retornados pela base consultada.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DEMOGRAPHIC_DATA_CPF",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Dados eleitorais de candidato

- Service: `SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna histórico de candidaturas eleitorais do CPF, incluindo cargo, partido, ano, unidade eleitoral, bens declarados e situação quando disponível.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Dados financeiros e endereços

- Service: `SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`
- Retorno principal: Retorna dados financeiros e endereços do CPF em uma consulta combinada, incluindo renda estimada, indicadores financeiros e endereços encontrados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Dados pelo telefone

- Service: `SERVICE_CONFIRM_PHONE_FACETEC`
- Endpoint: `POST /api/service-api`
- Campos do request: `phone`
- Retorno principal: Retorna dados associados ao telefone informado, como possível titular, documento relacionado, status de confirmacao e atributos disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CONFIRM_PHONE_FACETEC",
  "phone": "+5561123456789"
}'
```

### Dados PIS

- Service: `SERVICE_PIS_CONSULTATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna dados de PIS/NIS associados ao CPF, incluindo número encontrado, status, dados cadastrais relacionados e mensagens da consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PIS_CONSULTATION_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Dívida ativa

- Service: `SERVICE_ACTIVE_DEBT_PF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna dívidas ativas vinculadas ao CPF, com origem do débito, valores, situação, órgão credor e status da consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ACTIVE_DEBT_PF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Doações eleitorais

- Service: `SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna doações eleitorais realizadas pelo CPF, com ano, candidato/partido, valor, cargo, UF e detalhes da prestacao de contas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Documentoscopia digital

- Service: `SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX`
- Endpoint: `POST /api/service-api`
- Campos do request: `key`, `image1`, `image2`, `selfie1`
- Retorno principal: Retorna status da documentoscopia, chave da consulta, dados extraídos do documento, validações de documento/selfie e resultado de aprovacao.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX",
  "key": "84bfcd2e-2336-4e30-bcab-15348b7890b5",
  "image1": "base64",
  "image2": "base64",
  "selfie1": "base64"
}'
```

### Domínios

- Service: `SERVICE_DOMAINS_CPF`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna domínios, sites e sinais digitais associados ao CPF, incluindo quantidade e registros encontrados quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DOMAINS_CPF",
  "cpf": "cpf"
}'
```

### Endereços

- Service: `SERVICE_ADDRESS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna endereços associados ao CPF, incluindo logradouro, número, bairro, cidade, UF, CEP, país, tipo e indicadores de atualidade quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ADDRESS_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Enriquecimento de dados

- Service: `SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna dados cadastrais do CPF, incluindo nome, nascimento, situação cadastral, filiação, óbito, idade, gênero e atributos disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Envolvimento político

- Service: `SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna envolvimento político do CPF, incluindo candidaturas, cargos, doações, prestações de serviço, partidos e vínculos políticos.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Envolvimento político PF (BigDataCorp)

- Service: `SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna envolvimento político do CPF, incluindo candidaturas, cargos, doações, prestações de serviço, partidos e vínculos políticos.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Exposição e perfil na mídia

- Service: `SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna exposição e perfil de mídia da pessoa, com noticias, fontes, categorias, sentimento, relevancia e alertas encontrados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### FaceMatch

- Service: `SERVICE_FACE_MATCH_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `image1`, `image2`
- Retorno principal: Retorna comparacao facial entre duas imagens, com score de similaridade, status do match e mensagem de aprovacao ou reprovacao.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FACE_MATCH_BIGDATACORP",
  "image1": "base64",
  "image2": "base64"
}'
```

### FaceMatch (AWS)

- Service: `SERVICE_FACE_MATCH_AWS`
- Endpoint: `POST /api/service-api`
- Campos do request: `image1`, `image2`
- Retorno principal: Retorna comparacao facial entre duas imagens, com score de similaridade, status do match e mensagem de aprovacao ou reprovacao.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FACE_MATCH_AWS",
  "image1": "base64",
  "image2": "base64"
}'
```

### Histórico de e-mails

- Service: `SERVICE_EMAILS_EXTENDED_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `limit`
- Retorno principal: Retorna e-mails associados ao CPF, incluindo prioridade, status de validação, origem, data de atualização e sinais de uso quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_EMAILS_EXTENDED_BIGDATACORP",
  "cpf": "cpf",
  "limit": "10"
}'
```

### Histórico de telefones

- Service: `SERVICE_PHONE_HISTORY_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`, `limit`
- Retorno principal: Retorna histórico de telefones associados ao CPF, incluindo número, tipo de linha, operadora, prioridade, status e recência quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PHONE_HISTORY_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)",
  "limit": "10"
}'
```

### Histórico familiar político

- Service: `SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna histórico político familiar do CPF, incluindo familiares com candidaturas, doações, cargos, partidos e vínculos eleitorais quando encontrados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Histórico profissional

- Service: `SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna histórico profissional do CPF, incluindo empresas, cargos, datas, vínculos empregaticios ou societários e indicadores profissionais.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Histórico profissional do titular

- Service: `SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`
- Retorno principal: Retorna histórico profissional em que a pessoa aparece como titular, sócio ou proprietario, com empresas, cargos e datas de vinculo.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Indicadores de atividades

- Service: `SERVICE_ACTIVITIES_INDICATORS`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna indicadores de atividades vinculadas ao CPF, como sinais profissionais, segmentos, ocupacoes e registros disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ACTIVITIES_INDICATORS",
  "cpf": "cpf"
}'
```

### Informações financeiras

- Service: `SERVICE_FINANCIAL_INFORMATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna informações financeiras estimadas do CPF, como renda presumida, poder aquisitivo, classe econômica e indicadores financeiros disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FINANCIAL_INFORMATION_BIGDATACORP",
  "cpf": "cpf"
}'
```

### KYC e compliance

- Service: `SERVICE_PERSON_KYC_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`
- Retorno principal: Retorna checagem de KYC da pessoa, incluindo PEP, sanções, mídia, processos, alertas de compliance e sinais de risco.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_KYC_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Liveness 2D

- Service: `SERVICE_LIVENESS_2D_FACETEC`
- Endpoint: `POST /api/service-api`
- Campos do request: `image1`
- Retorno principal: Retorna resultado da prova de vida 2D, com status, score ou confiança da selfie e sinais de validação contra fraude simples.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_LIVENESS_2D_FACETEC",
  "image1": "selfie"
}'
```

### Mandado de prisão

- Service: `SERVICE_ARREST_WARRANT`
- Endpoint: `POST /api/service-api`
- Campos do request: `nome`, `motherName`, `fatherName`, `birthDate`, `cpf`
- Retorno principal: Retorna indicativos de mandado de prisão para os dados informados, com situação, órgão, processo e detalhes encontrados quando houver ocorrência.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ARREST_WARRANT",
  "nome": "nome",
  "motherName": "nome da mãe",
  "fatherName": "nome do pai",
  "birthDate": "dd/MM/yyyy",
  "cpf": "cpf"
}'
```

### Modelagem de dados

- Service: `SERVICE_PERSON_DATA_MODELING_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna modelagem consolidada da pessoa, reunindo dados cadastrais, contatos, endereços, vínculos, indicadores e resumos derivados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_DATA_MODELING_BIGDATACORP",
  "cpf": "cpf"
}'
```

### OCR de comprovante de endereço

- Service: `SERVICE_OCR_PROOF_OF_ADDRESS`
- Endpoint: `POST /api/service-api`
- Campos do request: `image1`
- Retorno principal: Retorna dados extraídos do comprovante de endereço por Textract, como texto OCR, nome, endereço, tipo do documento, datas e valores quando encontrados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OCR_PROOF_OF_ADDRESS",
  "image1": "base64"
}'
```

### OCR de documentos

- Service: `SERVICE_OCR_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `image1`, `image2`, `image1Url`, `image2Url`
- Retorno principal: Retorna dados extraídos das imagens do documento, como tipo documental, nome, CPF, nascimento, filiação, número do documento e campos específicos.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OCR_BIGDATACORP",
  "image1": "base64",
  "image2": "base64",
  "image1Url": "url_image",
  "image2Url": "urlImageMatch"
}'
```

### OCR de emancipação

- Service: `SERVICE_OCR_EMANCIPATION`
- Endpoint: `POST /api/service-api`
- Campos do request: `image1`
- Retorno principal: Retorna texto OCR do documento de emancipacao e dados objetivos extraídos quando existirem, sem reprovar pela ausencia de campos variaveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OCR_EMANCIPATION",
  "image1": "base64"
}'
```

### OCR React RG/CNH

- Service: `SERVICE_OCR`
- Endpoint: `POST /api/service-api`
- Campos do request: `documentType`, `image1`, `image2`
- Retorno principal: Retorna dados extraídos de RG ou CNH enviados por imagem, como CPF, nome, filiação, nascimento, órgão emissor e dados específicos do documento.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OCR",
  "documentType": "CNH",
  "image1": "base64",
  "image2": "base64 (obrigatorio para RG)"
}'
```

### Pessoa politicamente exposta

- Service: `SERVICE_PEP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna se o CPF e PEP ou relacionado a PEP, com cargo, órgão, nível de exposição, período e vínculos encontrados quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PEP",
  "cpf": "cpf"
}'
```

### Pessoas relacionadas

- Service: `SERVICE_RELATED_PEOPLE_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`
- Retorno principal: Retorna pessoas relacionadas ao CPF, com nome, documento mascarado, tipo de relação, nível de proximidade e origem do vinculo.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RELATED_PEOPLE_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Prêmios e certificações

- Service: `SERVICE_AWARDS_AND_CERTIFICATIONS_CPF`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna a quantidade e os registros de premios e certificacoes encontrados para o CPF, quando a base consultada possuir dados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_AWARDS_AND_CERTIFICATIONS_CPF",
  "cpf": "cpf"
}'
```

### Prestadores de serviço eleitorais

- Service: `SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna prestações de serviço eleitorais vinculadas ao CPF, com campanha, candidato/partido, valor, ano e natureza do serviço.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Processos jurídicos e administrativos

- Service: `SERVICE_JURIDICAL_PROCESSES_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna processos jurídicos e administrativos vinculados ao CPF, com tribunal, classe, assunto, partes, status e datas quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_JURIDICAL_PROCESSES_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Prompt de IA para pessoa

- Service: `SERVICE_PERSON_AI_PROMPT_OPENAI`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna uma resposta textual consolidada por IA a partir dos dados da pessoa, com resumo, pontos de atenção e leitura operacional.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_AI_PROMPT_OPENAI",
  "cpf": "cpf"
}'
```

### Propensão a apostas online

- Service: `SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna propensão do CPF a apostas online, com score, faixa de propensão, indicadores comportamentais e sinais associados quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Relacionamentos econômicos

- Service: `SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna vínculos econômicos associados ao CPF, como empresas relacionadas, participacoes, relacoes profissionais e indicadores de relacionamento.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Resultado da documentoscopia digital

- Service: `SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX`
- Endpoint: `POST /api/service-api`
- Campos do request: `key`
- Retorno principal: Retorna o resultado ja processado da documentoscopia pela chave informada, com status, campos extraídos, regras avaliadas e evidencias.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX",
  "key": "de0cd562-5962-40bd-8f94-5a7184ecde0e"
}'
```

### Risco financeiro

- Service: `SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`
- Retorno principal: Retorna score de risco financeiro do CPF, faixa de risco, recomendação resumida e fatores que influenciam a avaliação.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Score biométrico

- Service: `SERVICE_DATAVALID_CNH_SERPRO`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `image1`
- Retorno principal: Retorna validação DataValid/Serpro da CNH, incluindo score biométrico, similaridade facial, status de validação e campos conferidos.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DATAVALID_CNH_SERPRO",
  "cpf": "cpf",
  "image1": "{base64Image}"
}'
```

### Score de crédito

- Service: `SERVICE_CREDIT_SCORE`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna score de credito associado ao CPF via Assertiva, com pontuacao, faixa de risco e mensagem da consulta quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CREDIT_SCORE",
  "cpf": "cpf"
}'
```

### Score de inadimplência

- Service: `SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna score de inadimplência do CPF, faixa de risco, probabilidade estimada e indicadores usados na avaliação.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Score de risco de fraude

- Service: `SERVICE_FRAUD_RISK_SCORE_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `factor`
- Retorno principal: Retorna score de risco de fraude do CPF, fator analisado, nível de risco, score numérico e sinais que suportam a decisão.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FRAUD_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf",
  "factor": "minRisk or minattrition"
}'
```

### Score de risco Quantum

- Service: `SERVICE_DEFAULT_RISK_SCORE_QUANTUM`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna score de risco de inadimplência Quantum para CPF, com pontuacao, faixa de risco e probabilidade estimada quando disponível.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DEFAULT_RISK_SCORE_QUANTUM",
  "cpf": "cpf"
}'
```

### Servidores públicos

- Service: `SERVICE_PUBLIC_SERVANTS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna registros de servidor publico associados ao CPF, incluindo órgão, cargo, vinculo, remuneracao/faixa e período quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PUBLIC_SERVANTS_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Status do CPF na Receita Federal

- Service: `SERVICE_RFB_PF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `dataDeNascimento`
- Retorno principal: Retorna situação do CPF na Receita Federal, incluindo nome, nascimento, status cadastral, comprovante/protocolo e dados fiscais disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PF_BIGDATACORP",
  "cpf": "cpf",
  "dataDeNascimento": "yyyy-MM-dd (opcional)"
}'
```

### Validação de CPF com endereço

- Service: `SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `zipcode`, `numberAddress`
- Retorno principal: Retorna se o endereço informado tem associação com o CPF, incluindo nível de match, endereço normalizado e sinais usados na validação.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP",
  "cpf": "cpf",
  "zipcode": "00000-000",
  "numberAddress": "13"
}'
```

### Validação de CPF com telefone

- Service: `SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `phone`
- Retorno principal: Retorna se o telefone informado tem associação com o CPF, incluindo nível de match, tipo de linha, status e sinais de validação.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP",
  "cpf": "cpf",
  "phone": "11900000000"
}'
```

### Validação de CPF com telefone (Facetec)

- Service: `SERVICE_CPF_PHONE_VALIDATION_FACETEC`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `phone`
- Retorno principal: Retorna validação da associação entre CPF e telefone, com status de match, mensagem da consulta e dados retornados pelo parceiro.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CPF_PHONE_VALIDATION_FACETEC",
  "cpf": "cpf",
  "phone": "11900000000"
}'
```

### Validação de e-mail

- Service: `SERVICE_EMAIL_VALIDATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `email`
- Retorno principal: Retorna validação do e-mail informado, incluindo formato, existencia provável, domínio, entregabilidade e indicadores de risco.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_EMAIL_VALIDATION_BIGDATACORP",
  "email": "email@email.com"
}'
```

### Validação do E-Social

- Service: `SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `nit`
- Retorno principal: Retorna qualificação cadastral no eSocial, com status de consistencia entre CPF, NIT/PIS e dados cadastrais informados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP",
  "cpf": "cpf",
  "nit": "(opcional)"
}'
```

## Pessoa Jurídica

### Certidão negativa de protesto

- Service: `SERVICE_PROTEST_PJ_INFOSIMPLES`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna certidão/consulta de protestos para CNPJ via InfoSimples, com status, cartórios consultados, protestos, valores e datas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PJ_INFOSIMPLES",
  "cnpj": "cnpj"
}'
```

### Certidão negativa de protesto PJ (Netrin)

- Service: `SERVICE_PROTEST_PJ_NETRIN`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna certidão/consulta de protestos para CNPJ via Netrin, com status, cartórios consultados, protestos, valores e datas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PJ_NETRIN",
  "cnpj": "cnpj"
}'
```

### CNPJ na Receita Federal on-demand

- Service: `SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna situação atualizada do CNPJ consultada sob demanda na Receita Federal, com razão social, status cadastral, CNAEs e endereço.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Compliance de casas de apostas

- Service: `SERVICE_COMPLIANCE_BET`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna indicadores de exposição da empresa a apostas, bets e compliance regulatório, incluindo sinais de operação, domínio, atividade e alertas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPLIANCE_BET",
  "cnpj": "cnpj"
}'
```

### Compliance de casas de apostas

- Service: `SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna indicadores de exposição da empresa a apostas, bets e compliance regulatório, incluindo sinais de operação, domínio, atividade e alertas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Dados cadastrais de CNPJ

- Service: `SERVICE_REGISTRATION_DATA_CNPJ`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna dados cadastrais do CNPJ, incluindo razão social, nome fantasia, situação, abertura, CNAEs, natureza jurídica e endereço quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_REGISTRATION_DATA_CNPJ",
  "cnpj": "cnpj"
}'
```

### DAS MEI na Receita

- Service: `SERVICE_DAS_MEI_INFOSIMPLES`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna informações de DAS MEI e situação fiscal relacionada ao CNPJ, incluindo períodos, pagamentos, pendências e status quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DAS_MEI_INFOSIMPLES",
  "cnpj": "cnpj"
}'
```

### Débitos ativos

- Service: `SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna dívidas ativas vinculadas ao CNPJ, com origem do débito, valores, situação, órgão credor e status da consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Doações eleitorais

- Service: `SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna doações eleitorais realizadas pela empresa, com ano, candidato/partido, valor, cargo, UF e detalhes da prestacao de contas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Doações eleitorais dos sócios

- Service: `SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna doações eleitorais feitas pelos sócios da empresa, com sócio relacionado, ano, candidato/partido, valor e detalhes eleitorais.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Domínios CNPJ

- Service: `SERVICE_DOMAINS_CNPJ`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna domínios, sites e sinais digitais associados ao CNPJ, incluindo quantidade e registros encontrados quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DOMAINS_CNPJ",
  "cnpj": "cnpj"
}'
```

### Endereços estendidos

- Service: `SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna endereços associados ao CNPJ, incluindo logradouro, bairro, cidade, UF, CEP, país, tipo de endereço e dados complementares quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Enriquecimento de dados

- Service: `SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna cadastro completo da empresa, incluindo razão social, nome fantasia, situação cadastral, CNAEs, natureza jurídica, porte, capital e endereço.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Exposição e perfil na mídia dos sócios

- Service: `SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna exposição e perfil de mídia da empresa e sócios, com noticias, fontes, categorias, sentimento, relevancia e alertas encontrados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Fornecedores eleitorais

- Service: `SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna prestações de serviço eleitorais vinculadas ao CNPJ, com campanha, candidato/partido, valor, ano e natureza do serviço.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### KYC e compliance dos sócios

- Service: `SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna checagens de KYC e compliance dos sócios da empresa, incluindo PEP, sanções, mídia, risco e alertas encontrados por sócio.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### OCR de cartão CNPJ

- Service: `SERVICE_OCR_CNPJ_CARD`
- Endpoint: `POST /api/service-api`
- Campos do request: `image1`
- Retorno principal: Retorna dados extraídos do cartao CNPJ enviado por imagem, incluindo CNPJ, tipo do documento e texto OCR quando disponível.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OCR_CNPJ_CARD",
  "image1": "base64"
}'
```

### Processos jurídicos

- Service: `SERVICE_JURIDICAL_PROCESSES_PJ`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna processos jurídicos vinculados ao CNPJ, com tribunal, classe, assunto, partes, status, número do processo e datas quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_JURIDICAL_PROCESSES_PJ",
  "cnpj": "cnpj"
}'
```

### Processos jurídicos dos sócios

- Service: `SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna processos jurídicos associados aos sócios da empresa, com sócio relacionado, tribunal, classe, assunto, status e datas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Relacionamentos da empresa

- Service: `SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna relacionamentos da empresa, como sócios, proprietários, empresas relacionadas, participacoes e vínculos societários identificados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Risco de crédito

- Service: `SERVICE_CREDIT_RISK_COMPANY`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna dados publicos de risco de credito da empresa, como status, score, rating, risco esperado e processos legais quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CREDIT_RISK_COMPANY",
  "cnpj": "cnpj"
}'
```

### Risco de crédito Murabei

- Service: `SERVICE_CREDIT_RISK_COMPANY_MURABEI`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna dados de risco de credito PJ no fluxo Murabei, com score, rating, risco esperado e sinais jurídicos quando disponíveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CREDIT_RISK_COMPANY_MURABEI",
  "cnpj": "cnpj"
}'
```

### SINTEGRA

- Service: `SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`, `uf`
- Retorno principal: Retorna dados do SINTEGRA, incluindo inscrição estadual, UF, situação, regime, atividades, endereço e mensagens da consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP",
  "cnpj": "cnpj",
  "uf": "uf (opcional)"
}'
```

### Sócios de primeiro nível

- Service: `SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna sócios de primeiro nível da empresa, com nome, documento, participação, qualificação e vínculos diretos ao CNPJ.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Sócios na Receita Federal

- Service: `SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna o quadro societario na Receita Federal, com nome dos sócios, documentos mascarados, qualificação, participação e data de entrada quando disponível.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Status do CNPJ na Receita Federal

- Service: `SERVICE_RFB_PJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna situação do CNPJ na Receita Federal, incluindo razão social, nome fantasia, situação cadastral, abertura, CNAEs e endereço.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```
