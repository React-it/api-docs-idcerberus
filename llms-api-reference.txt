# idCerberus API Reference - resumo operacional para LLM

Use este arquivo para gerar exemplos de request, explicar chamadas da API e escolher o `service` correto sem depender do OpenAPI completo.

## Regras obrigatórias

- Não invente endpoints, parâmetros ou services.
- Para consultas externas, use `POST /api/service-api` e envie o campo `service` no body.
- Use homologação para testes: `https://backoffice-hml.idcerberus.com`.
- Use produção somente quando o usuário pedir explicitamente: `https://backoffice.idcerberus.com`.
- Nunca exponha `client`, `secret`, JWT real, CPF real, CNPJ real ou imagens reais em exemplos.
- Quando faltar um service no catálogo, diga que ele precisa ser confirmado antes de documentar ou integrar.

## Autenticação

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/token-generate' \
--header 'Content-Type: application/json' \
--data '{
  "client": "{client}",
  "secret": "{secret}"
}'
```

## Pessoa Física

### Antecedentes criminais civis

- Service: `SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `rg`, `uf`
- Retorno principal: Retorna resultado de antecedentes criminais civis, com status da certidao, ocorrencias encontradas, UF, RG e mensagens da consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CRIMINAL_RECORD_CIVIL_BIGDATACORP",
  "cpf": "cpf",
  "rg": "rg",
  "uf": "uf"
}'
```

### Antecedentes criminais federais

- Service: `SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna resultado de antecedentes criminais federais, com status da certidao, ocorrencias encontradas e mensagens da consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CRIMINAL_RECORD_FEDERAL_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Certidão de Nada Consta

- Service: `SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `court`, `uf`, `sphere`
- Retorno principal: Retorna certidao de nada consta para a esfera/tribunal informado, com status, mensagem, ocorrencias e dados usados na consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_NOTHING_RECORD_LAWSUITS_BIGDATACORP",
  "cpf": "cpf",
  "court": "TRF1",
  "uf": "uf",
  "sphere": "CIVIL"
}'
```

### Certidão negativa de protesto

- Service: `SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna certidao/consulta de protestos para CPF, com status de nada consta ou lista de protestos, cartorio, valor e datas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_CLEARANCE_CERTIFICATE_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Certidão negativa de protesto PF (InfoSimples)

- Service: `SERVICE_PROTEST_PF_INFOSIMPLES`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna certidao/consulta de protestos para CPF via InfoSimples, com status, cartorios consultados, protestos e mensagens.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PF_INFOSIMPLES",
  "cpf": "cpf"
}'
```

### Certidão negativa de protesto PF (Netrin)

- Service: `SERVICE_PROTEST_PF_NETRIN`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna certidao/consulta de protestos para CPF via Netrin, com status, cartorios consultados, protestos e mensagens.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PF_NETRIN",
  "cpf": "cpf"
}'
```

### Consulta de MEI

- Service: `SERVICE_MEI_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna empresas MEI associadas ao CPF, incluindo CNPJ, razao social, situacao, atividades, endereco e datas cadastrais quando disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEI_BIGDATACORP",
  "cpf": "cpf"
}'
```

### CPF na Receita Federal on-demand

- Service: `SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna situacao atualizada do CPF consultada sob demanda na Receita Federal, com nome, nascimento, status cadastral e protocolo.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PF_ON_DEMAND_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Dados eleitorais de candidato

- Service: `SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna historico de candidaturas eleitorais do CPF, incluindo cargo, partido, ano, unidade eleitoral, bens declarados e situacao quando disponivel.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTION_CANDIDATE_DATA_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Dados financeiros e endereços

- Service: `SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`
- Retorno principal: Retorna dados financeiros e enderecos do CPF em uma consulta combinada, incluindo renda estimada, indicadores financeiros e enderecos encontrados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PF_FINANCIAL_AND_ADDRESS_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Dados pelo telefone

- Service: `SERVICE_CONFIRM_PHONE_FACETEC`
- Endpoint: `POST /api/service-api`
- Campos do request: `phone`
- Retorno principal: Retorna dados associados ao telefone informado, como possivel titular, documento relacionado, status de confirmacao e atributos disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CONFIRM_PHONE_FACETEC",
  "phone": "+5561123456789"
}'
```

### Dados PIS

- Service: `SERVICE_PIS_CONSULTATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna dados de PIS/NIS associados ao CPF, incluindo numero encontrado, status, dados cadastrais relacionados e mensagens da consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PIS_CONSULTATION_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Dívida ativa

- Service: `SERVICE_ACTIVE_DEBT_PF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna dividas ativas vinculadas ao CPF, com origem do debito, valores, situacao, orgao credor e status da consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ACTIVE_DEBT_PF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Doações eleitorais

- Service: `SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna doacoes eleitorais realizadas pelo CPF, com ano, candidato/partido, valor, cargo, UF e detalhes da prestacao de contas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_DONORS_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Documentoscopia digital

- Service: `SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX`
- Endpoint: `POST /api/service-api`
- Campos do request: `key`, `image1`, `image2`, `selfie1`
- Retorno principal: Retorna status da documentoscopia, chave da consulta, dados extraidos do documento, validacoes de documento/selfie e resultado de aprovacao.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DIGITAL_DOCUMENTOSCOPY_ACERTPIX",
  "key": "84bfcd2e-2336-4e30-bcab-15348b7890b5",
  "image1": "base64",
  "image2": "base64",
  "selfie1": "base64"
}'
```

### Endereços

- Service: `SERVICE_ADDRESS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna enderecos associados ao CPF, incluindo logradouro, numero, bairro, cidade, UF, CEP, pais, tipo e indicadores de atualidade quando disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ADDRESS_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Enriquecimento de dados

- Service: `SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna dados cadastrais do CPF, incluindo nome, nascimento, situacao cadastral, filiacao, obito, idade, genero e atributos disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_DATA_ENRICHMENT_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Envolvimento político

- Service: `SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna envolvimento politico do CPF, incluindo candidaturas, cargos, doacoes, prestacoes de servico, partidos e vinculos politicos.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_POLITICAL_INVOLVEMENT_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Envolvimento político PF (BigDataCorp)

- Service: `SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna envolvimento politico do CPF, incluindo candidaturas, cargos, doacoes, prestacoes de servico, partidos e vinculos politicos.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_POLITICAL_INVOLVEMENT_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Exposição e perfil na mídia

- Service: `SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna exposicao e perfil de midia da pessoa, com noticias, fontes, categorias, sentimento, relevancia e alertas encontrados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### FaceMatch

- Service: `SERVICE_FACE_MATCH_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `image1`, `image2`
- Retorno principal: Retorna comparacao facial entre duas imagens, com score de similaridade, status do match e mensagem de aprovacao ou reprovacao.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FACE_MATCH_BIGDATACORP",
  "image1": "base64",
  "image2": "base64"
}'
```

### FaceMatch (AWS)

- Service: `SERVICE_FACE_MATCH_AWS`
- Endpoint: `POST /api/service-api`
- Campos do request: `image1`, `image2`
- Retorno principal: Retorna comparacao facial entre duas imagens, com score de similaridade, status do match e mensagem de aprovacao ou reprovacao.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FACE_MATCH_AWS",
  "image1": "base64",
  "image2": "base64"
}'
```

### Histórico de e-mails

- Service: `SERVICE_EMAILS_EXTENDED_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `limit`
- Retorno principal: Retorna e-mails associados ao CPF, incluindo prioridade, status de validacao, origem, data de atualizacao e sinais de uso quando disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_EMAILS_EXTENDED_BIGDATACORP",
  "cpf": "cpf",
  "limit": "10"
}'
```

### Histórico de telefones

- Service: `SERVICE_PHONE_HISTORY_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`, `limit`
- Retorno principal: Retorna historico de telefones associados ao CPF, incluindo numero, tipo de linha, operadora, prioridade, status e recencia quando disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PHONE_HISTORY_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)",
  "limit": "10"
}'
```

### Histórico familiar político

- Service: `SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna historico politico familiar do CPF, incluindo familiares com candidaturas, doacoes, cargos, partidos e vinculos eleitorais quando encontrados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FAMILY_POLITICAL_HISTORY_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Histórico profissional

- Service: `SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna historico profissional do CPF, incluindo empresas, cargos, datas, vinculos empregaticios ou societarios e indicadores profissionais.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROFESSIONAL_HISTORY_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Histórico profissional do titular

- Service: `SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`
- Retorno principal: Retorna historico profissional em que a pessoa aparece como titular, socio ou proprietario, com empresas, cargos e datas de vinculo.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROFESSIONAL_HISTORY_OWNER_ONLY_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Informações financeiras

- Service: `SERVICE_FINANCIAL_INFORMATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna informacoes financeiras estimadas do CPF, como renda presumida, poder aquisitivo, classe economica e indicadores financeiros disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FINANCIAL_INFORMATION_BIGDATACORP",
  "cpf": "cpf"
}'
```

### KYC e compliance

- Service: `SERVICE_PERSON_KYC_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`
- Retorno principal: Retorna checagem de KYC da pessoa, incluindo PEP, sancoes, midia, processos, alertas de compliance e sinais de risco.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_KYC_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Liveness 2D

- Service: `SERVICE_LIVENESS_2D_FACETEC`
- Endpoint: `POST /api/service-api`
- Campos do request: `image1`
- Retorno principal: Retorna resultado da prova de vida 2D, com status, score ou confianca da selfie e sinais de validacao contra fraude simples.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_LIVENESS_2D_FACETEC",
  "image1": "selfie"
}'
```

### Mandado de prisão

- Service: `SERVICE_ARREST_WARRANT`
- Endpoint: `POST /api/service-api`
- Campos do request: `nome`, `motherName`, `fatherName`, `birthDate`, `cpf`
- Retorno principal: Retorna indicativos de mandado de prisao para os dados informados, com situacao, orgao, processo e detalhes encontrados quando houver ocorrencia.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ARREST_WARRANT",
  "nome": "nome",
  "motherName": "nome da mãe",
  "fatherName": "nome do pai",
  "birthDate": "dd/MM/yyyy",
  "cpf": "cpf"
}'
```

### Modelagem de dados

- Service: `SERVICE_PERSON_DATA_MODELING_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna modelagem consolidada da pessoa, reunindo dados cadastrais, contatos, enderecos, vinculos, indicadores e resumos derivados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_DATA_MODELING_BIGDATACORP",
  "cpf": "cpf"
}'
```

### OCR de documentos

- Service: `SERVICE_OCR_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `image1`, `image2`, `image1Url`, `image2Url`
- Retorno principal: Retorna dados extraidos das imagens do documento, como tipo documental, nome, CPF, nascimento, filiacao, numero do documento e campos especificos.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OCR_BIGDATACORP",
  "image1": "base64",
  "image2": "base64",
  "image1Url": "url_image",
  "image2Url": "urlImageMatch"
}'
```

### Pessoa politicamente exposta

- Service: `SERVICE_PEP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna se o CPF e PEP ou relacionado a PEP, com cargo, orgao, nivel de exposicao, periodo e vinculos encontrados quando disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PEP",
  "cpf": "cpf"
}'
```

### Pessoas relacionadas

- Service: `SERVICE_RELATED_PEOPLE_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`
- Retorno principal: Retorna pessoas relacionadas ao CPF, com nome, documento mascarado, tipo de relacao, nivel de proximidade e origem do vinculo.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RELATED_PEOPLE_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Prestadores de serviço eleitorais

- Service: `SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna prestacoes de servico eleitorais vinculadas ao CPF, com campanha, candidato/partido, valor, ano e natureza do servico.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CPF_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Processos jurídicos e administrativos

- Service: `SERVICE_JURIDICAL_PROCESSES_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna processos juridicos e administrativos vinculados ao CPF, com tribunal, classe, assunto, partes, status e datas quando disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_JURIDICAL_PROCESSES_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Prompt de IA para pessoa

- Service: `SERVICE_PERSON_AI_PROMPT_OPENAI`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna uma resposta textual consolidada por IA a partir dos dados da pessoa, com resumo, pontos de atencao e leitura operacional.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PERSON_AI_PROMPT_OPENAI",
  "cpf": "cpf"
}'
```

### Propensão a apostas online

- Service: `SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna propensao do CPF a apostas online, com score, faixa de propensao, indicadores comportamentais e sinais associados quando disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SEVICE_ONLINE_BETTING_PROPENSITY_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Relacionamentos econômicos

- Service: `SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna vinculos economicos associados ao CPF, como empresas relacionadas, participacoes, relacoes profissionais e indicadores de relacionamento.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ECONOMIC_RELATIONSHIP_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Resultado da documentoscopia digital

- Service: `SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX`
- Endpoint: `POST /api/service-api`
- Campos do request: `key`
- Retorno principal: Retorna o resultado ja processado da documentoscopia pela chave informada, com status, campos extraidos, regras avaliadas e evidencias.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DIGITAL_DOCUMENTOSCOPY_CONSULT_ACERTPIX",
  "key": "de0cd562-5962-40bd-8f94-5a7184ecde0e"
}'
```

### Risco financeiro

- Service: `SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `birthDate`
- Retorno principal: Retorna score de risco financeiro do CPF, faixa de risco, recomendacao resumida e fatores que influenciam a avaliacao.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FINANCIAL_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf",
  "birthDate": "yyyy-MM-dd (opcional)"
}'
```

### Score biométrico

- Service: `SERVICE_DATAVALID_CNH_SERPRO`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `image1`
- Retorno principal: Retorna validacao DataValid/Serpro da CNH, incluindo score biometrico, similaridade facial, status de validacao e campos conferidos.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DATAVALID_CNH_SERPRO",
  "cpf": "cpf",
  "image1": "{base64Image}"
}'
```

### Score de inadimplência

- Service: `SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna score de inadimplencia do CPF, faixa de risco, probabilidade estimada e indicadores usados na avaliacao.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DEFAULT_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Score de risco de fraude

- Service: `SERVICE_FRAUD_RISK_SCORE_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `factor`
- Retorno principal: Retorna score de risco de fraude do CPF, fator analisado, nivel de risco, score numerico e sinais que suportam a decisao.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FRAUD_RISK_SCORE_BIGDATACORP",
  "cpf": "cpf",
  "factor": "minRisk or minattrition"
}'
```

### Servidores públicos

- Service: `SERVICE_PUBLIC_SERVANTS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`
- Retorno principal: Retorna registros de servidor publico associados ao CPF, incluindo orgao, cargo, vinculo, remuneracao/faixa e periodo quando disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PUBLIC_SERVANTS_BIGDATACORP",
  "cpf": "cpf"
}'
```

### Status do CPF na Receita Federal

- Service: `SERVICE_RFB_PF_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `dataDeNascimento`
- Retorno principal: Retorna situacao do CPF na Receita Federal, incluindo nome, nascimento, status cadastral, comprovante/protocolo e dados fiscais disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PF_BIGDATACORP",
  "cpf": "cpf",
  "dataDeNascimento": "yyyy-MM-dd (opcional)"
}'
```

### Validação de CPF com endereço

- Service: `SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `zipcode`, `numberAddress`
- Retorno principal: Retorna se o endereco informado tem associacao com o CPF, incluindo nivel de match, endereco normalizado e sinais usados na validacao.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CPF_ADDRESS_VALIDATION_BIGDATACORP",
  "cpf": "cpf",
  "zipcode": "00000-000",
  "numberAddress": "13"
}'
```

### Validação de CPF com telefone

- Service: `SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `phone`
- Retorno principal: Retorna se o telefone informado tem associacao com o CPF, incluindo nivel de match, tipo de linha, status e sinais de validacao.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CPF_PHONE_VALIDATION_BIGDATACORP",
  "cpf": "cpf",
  "phone": "11900000000"
}'
```

### Validação de CPF com telefone (Facetec)

- Service: `SERVICE_CPF_PHONE_VALIDATION_FACETEC`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `phone`
- Retorno principal: Retorna validacao da associacao entre CPF e telefone, com status de match, mensagem da consulta e dados retornados pelo parceiro.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CPF_PHONE_VALIDATION_FACETEC",
  "cpf": "cpf",
  "phone": "11900000000"
}'
```

### Validação de e-mail

- Service: `SERVICE_EMAIL_VALIDATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `email`
- Retorno principal: Retorna validacao do e-mail informado, incluindo formato, existencia provavel, dominio, entregabilidade e indicadores de risco.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_EMAIL_VALIDATION_BIGDATACORP",
  "email": "email@email.com"
}'
```

### Validação do E-Social

- Service: `SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cpf`, `nit`
- Retorno principal: Retorna qualificacao cadastral no eSocial, com status de consistencia entre CPF, NIT/PIS e dados cadastrais informados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ESOCIAL_REGISTRATION_QUALIFICATION_BIGDATACORP",
  "cpf": "cpf",
  "nit": "(opcional)"
}'
```

## Pessoa Jurídica

### Certidão negativa de protesto

- Service: `SERVICE_PROTEST_PJ_INFOSIMPLES`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna certidao/consulta de protestos para CNPJ via InfoSimples, com status, cartorios consultados, protestos, valores e datas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PJ_INFOSIMPLES",
  "cnpj": "cnpj"
}'
```

### Certidão negativa de protesto PJ (Netrin)

- Service: `SERVICE_PROTEST_PJ_NETRIN`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna certidao/consulta de protestos para CNPJ via Netrin, com status, cartorios consultados, protestos, valores e datas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_PROTEST_PJ_NETRIN",
  "cnpj": "cnpj"
}'
```

### CNPJ na Receita Federal on-demand

- Service: `SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna situacao atualizada do CNPJ consultada sob demanda na Receita Federal, com razao social, status cadastral, CNAEs e endereco.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PJ_ON_DEMAND_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Compliance de casas de apostas

- Service: `SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna indicadores de exposicao da empresa a apostas, bets e compliance regulatorio, incluindo sinais de operacao, dominio, atividade e alertas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPLIANCE_BET_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### DAS MEI na Receita

- Service: `SERVICE_DAS_MEI_INFOSIMPLES`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna informacoes de DAS MEI e situacao fiscal relacionada ao CNPJ, incluindo periodos, pagamentos, pendencias e status quando disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_DAS_MEI_INFOSIMPLES",
  "cnpj": "cnpj"
}'
```

### Débitos ativos

- Service: `SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna dividas ativas vinculadas ao CNPJ, com origem do debito, valores, situacao, orgao credor e status da consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ACTIVE_DEBT_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Doações eleitorais

- Service: `SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna doacoes eleitorais realizadas pela empresa, com ano, candidato/partido, valor, cargo, UF e detalhes da prestacao de contas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_DONORS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Doações eleitorais dos sócios

- Service: `SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna doacoes eleitorais feitas pelos socios da empresa, com socio relacionado, ano, candidato/partido, valor e detalhes eleitorais.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_OWNERS_ELECTORAL_DONORS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Endereços estendidos

- Service: `SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna enderecos associados ao CNPJ, incluindo logradouro, bairro, cidade, UF, CEP, pais, tipo de endereco e dados complementares quando disponiveis.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ADDRESSES_EXTENDED_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Enriquecimento de dados

- Service: `SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna cadastro completo da empresa, incluindo razao social, nome fantasia, situacao cadastral, CNAEs, natureza juridica, porte, capital e endereco.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_CORPORATE_DATA_ENRICHMENT_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Exposição e perfil na mídia dos sócios

- Service: `SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna exposicao e perfil de midia da empresa e socios, com noticias, fontes, categorias, sentimento, relevancia e alertas encontrados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_MEDIA_PROFILE_EXPOSURE_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Fornecedores eleitorais

- Service: `SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna prestacoes de servico eleitorais vinculadas ao CNPJ, com campanha, candidato/partido, valor, ano e natureza do servico.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_ELECTORAL_PROVIDERS_CNPJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### KYC e compliance dos sócios

- Service: `SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna checagens de KYC e compliance dos socios da empresa, incluindo PEP, sancoes, midia, risco e alertas encontrados por socio.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_KYC_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Processos jurídicos dos sócios

- Service: `SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna processos juridicos associados aos socios da empresa, com socio relacionado, tribunal, classe, assunto, status e datas.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_JURIDICAL_PROCESSES_PJ_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Relacionamentos da empresa

- Service: `SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna relacionamentos da empresa, como socios, proprietarios, empresas relacionadas, participacoes e vinculos societarios identificados.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_RELATIONSHIP_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### SINTEGRA

- Service: `SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`, `uf`
- Retorno principal: Retorna dados do SINTEGRA, incluindo inscricao estadual, UF, situacao, regime, atividades, endereco e mensagens da consulta.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_SINTEGRA_CONSULTATION_BIGDATACORP",
  "cnpj": "cnpj",
  "uf": "uf (opcional)"
}'
```

### Sócios de primeiro nível

- Service: `SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna socios de primeiro nivel da empresa, com nome, documento, participacao, qualificacao e vinculos diretos ao CNPJ.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_FIRST_LEVEL_PARTNER_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Sócios na Receita Federal

- Service: `SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna o quadro societario na Receita Federal, com nome dos socios, documentos mascarados, qualificacao, participacao e data de entrada quando disponivel.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_COMPANY_RFB_OWNERS_BIGDATACORP",
  "cnpj": "cnpj"
}'
```

### Status do CNPJ na Receita Federal

- Service: `SERVICE_RFB_PJ_BIGDATACORP`
- Endpoint: `POST /api/service-api`
- Campos do request: `cnpj`
- Retorno principal: Retorna situacao do CNPJ na Receita Federal, incluindo razao social, nome fantasia, situacao cadastral, abertura, CNAEs e endereco.

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {jwt_token}' \
--data '{
  "service": "SERVICE_RFB_PJ_BIGDATACORP",
  "cnpj": "cnpj"
}'
```
