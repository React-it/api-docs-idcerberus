---
title: Boas-vindas
description: Integre onboarding, KYC, biometria, OCR, risco e compliance com a API idCerberus
---

# API idCerberus

Integre consultas de CPF, CNPJ, OCR, biometria, risco, compliance e onboarding digital em um fluxo único de API.

Esta documentação foi organizada para dois momentos: testar rápido em homologação e implementar com segurança em produção.

<CardGroup cols={3}>
  <Card icon="rocket" title="Testar em 5 minutos" href="/guides/postman-do-zero">
    Configure token, ambiente e primeira chamada no Postman sem escrever código. Prefere não instalar nada? Use o painel de teste embutido em qualquer página da [API Reference](/api-reference/autorização/gerar-token-de-api).
  </Card>
  <Card icon="list-check" title="Receitas de teste" href="/guides/receitas-prontas">
    Copie fluxos prontos para CPF, CNPJ, OCR, Face Index, risco PJ e score PF.
  </Card>
  <Card icon="server" title="Executar um service" href="/api-reference/como-executar-service">
    Entenda o contrato do `POST /api/service-api` e copie payloads prontos.
  </Card>
  <Card icon="image" title="OCR e imagem" href="/guides/service-api/sobre-ocr-service-api">
    Veja como testar CNH, RG, cartão CNPJ, comprovante de endereço e documentos com base64.
  </Card>
  <Card icon="book" title="Catálogo de services" href="/guides/indice-de-services">
    Pesquise por alias, documento, categoria, campo ou objetivo de negócio.
  </Card>
  <Card icon="magnifying-glass" title="Busca rápida" href="/guides/busca-rapida">
    Encontre services por CPF, CNPJ, OCR, Face Index, erro, campo obrigatório ou alias.
  </Card>
  <Card icon="compass" title="Services por caso de uso" href="/api-reference/services-por-caso-de-uso">
    Escolha o service certo a partir do que você precisa validar ou consultar.
  </Card>
  <Card icon="triangle-exclamation" title="Erros comuns" href="/guides/erros-comuns-integracao">
    Diagnóstico rápido para token, acesso, payload, imagem, retorno vazio e falha externa.
  </Card>
  <Card icon="circle-question" title="Dúvidas e Soluções" href="/guides/duvidas-e-solucoes">
    Perguntas e respostas diretas por tema, antes de abrir chamado de suporte.
  </Card>
  <Card icon="bolt" title="Webhooks" href="/guides/webhooks">
    Receba aviso automático quando um onboarding mudar de status, sem polling.
  </Card>
</CardGroup>

## Caminho mais curto (consulta avulsa via API)

<Steps>
  <Step title="Gere um token">
    Use `POST /api/token-generate` com `client` e `secret` do produto.
  </Step>
  <Step title="Escolha o service">
    Abra o [índice de services](/guides/indice-de-services) ou a [API Reference](/api-reference/services-por-caso-de-uso) e copie o alias correto.
  </Step>
  <Step title="Monte o payload">
    Envie `service` e os campos obrigatórios do produto, como `cpf`, `cnpj`, `image1`, `image2`, `documentType` ou `key`.
  </Step>
  <Step title="Execute em HML">
    Chame `POST https://backoffice-hml.idcerberus.com/api/service-api` com `Authorization: Bearer {jwt_token}`.
  </Step>
  <Step title="Leia o resultado">
    Use `result` como retorno de negócio e `status.message`, `onboardingStatus` e `externalId` para diagnóstico.
  </Step>
</Steps>

<Info>
  Vai capturar documento e selfie do usuário num cadastro guiado, em vez de
  fazer consultas avulsas? Esse é o fluxo de onboarding via SDK ou Cliente
  Web. Veja [Onboarding via SDK](/guides/onboarding-sdk) em vez dos passos
  acima.
</Info>

## Mais usados

| Preciso testar | Onde ir | Exemplo pronto |
| --- | --- | --- |
| Gerar token | [Autenticação](/guides/autenticacao) | [`auth.hml.curl`](/examples/auth.hml.curl) |
| Consultar CPF | [Services de pessoa física](/api-reference/services-pessoa-fisica) | [`service-api-cpf.hml.curl`](/examples/service-api-cpf.hml.curl) |
| Consultar CNPJ | [Services de pessoa jurídica](/api-reference/services-pessoa-juridica) | [`service-api-cnpj.hml.curl`](/examples/service-api-cnpj.hml.curl) |
| OCR CNH | [OCR via Service API](/guides/service-api/sobre-ocr-service-api) | [`service-api-ocr-cnh.hml.curl`](/examples/service-api-ocr-cnh.hml.curl) |
| OCR RG | [OCR via Service API](/guides/service-api/sobre-ocr-service-api) | [`service-api-ocr-rg.hml.curl`](/examples/service-api-ocr-rg.hml.curl) |
| OCR cartão CNPJ | [OCR via Service API](/guides/service-api/sobre-ocr-service-api) | [`service-api-ocr-cnpj-card.hml.curl`](/examples/service-api-ocr-cnpj-card.hml.curl) |
| OCR comprovante | [OCR via Service API](/guides/service-api/sobre-ocr-service-api) | [`service-api-ocr-proof-of-address.hml.curl`](/examples/service-api-ocr-proof-of-address.hml.curl) |
| Face Index | [Services de pessoa física](/api-reference/services-pessoa-fisica) | [`service-api-face-index.hml.curl`](/examples/service-api-face-index.hml.curl) |
| Risco de crédito PJ | [Services de pessoa jurídica](/api-reference/services-pessoa-juridica) | [`service-api-credit-risk-company.hml.curl`](/examples/service-api-credit-risk-company.hml.curl) |

## Modelos de integração

| Modelo | Quando usar | O que você implementa |
| --- | --- | --- |
| API direta | Consultas server-to-server por CPF, CNPJ, imagem ou chave | Token, payload, chamada HTTP e leitura do `result` |
| SDK | Captura guiada de documento, selfie e dados no aplicativo | Geração do `tokenOnboarding` e consulta do resultado (ou recebimento via webhook) |
| Cliente Web | Fluxos em WebView, navegador ou QR Code | Redirecionamento do usuário e leitura do processamento |
| Webhook | Complementa qualquer modelo acima: avisa sua aplicação quando o status mudar, seja onboarding ou consulta avulsa | Configurar a URL de recebimento e validar o header `id-cerberus-token` |

## Endpoints essenciais

| Operação | Endpoint | Uso |
| --- | --- | --- |
| Gerar token | `POST /api/token-generate` | Autenticar chamadas protegidas |
| Executar service | `POST /api/service-api` | Rodar CPF, CNPJ, OCR, biometria, risco e compliance |
| Gerar TokenOnboarding | `POST /api/token-history-onboarding` | Iniciar fluxo via SDK |
| Consultar onboarding | `GET /api/onboarding/report/{tokenOnboarding}` | Obter status, campos e regras processadas |
| Baixar relatório PDF | `GET /api/history-onboarding-report/{tokenOnboarding}` | Armazenar ou auditar o resultado final |

## Ambientes

| Ambiente | Base URL |
| --- | --- |
| Homologação | `https://backoffice-hml.idcerberus.com` |
| Produção | `https://backoffice.idcerberus.com` |

## Exemplo rápido

Gere um token:

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/token-generate' \
  --header 'Content-Type: application/json' \
  --data '{
    "client": "{client}",
    "secret": "{secret}"
  }'
```

Use o token para executar uma consulta:

```bash
curl --location 'https://backoffice-hml.idcerberus.com/api/service-api' \
  --header 'Authorization: Bearer {jwt_token}' \
  --header 'Content-Type: application/json' \
  --data '{
    "service": "SERVICE_PERSON_DATA_ENRICHMENT",
    "cpf": "00000000000"
  }'
```

## Padrão de resposta

A maior parte dos services retorna os dados úteis em `result` e o status técnico em `status`.

```json
{
  "result": {},
  "status": {
    "code": 200,
    "message": "Success"
  },
  "onboardingStatus": "APPROVED",
  "externalId": "..."
}
```

<Note>
  O campo `service` define qual produto será executado dentro de `POST /api/service-api`. Os demais campos mudam conforme a consulta escolhida.
</Note>

## Próximos passos

1. Para testar no Postman, veja [Postman do zero](/guides/postman-do-zero).
2. Para copiar fluxos completos, veja [Receitas prontas](/guides/receitas-prontas).
3. Para OCR, base64 e imagens, veja [OCR via Service API](/guides/service-api/sobre-ocr-service-api).
4. Para escolher o service certo, veja [Services por caso de uso](/api-reference/services-por-caso-de-uso).
5. Para diagnosticar falhas, veja [Erros comuns de integração](/guides/erros-comuns-integracao).
